# Theseus refactor — full review findings (raw per-finding detail)

> Companion evidence appendix to `theseus_review_findings.md`. Harvested from workflow `wf_6d7ad80d-607` (12 finders + adversarial verifiers).
> 76 findings across 13 dimensions — 57 confirmed, 6 uncertain/pending, 13 refuted. Severities shown are verifier-corrected.

## Confirmed (verifier-survived), severity-ranked

### [CRITICAL] publish_internal drops boundary-mint / scheduled-activation intervals and mis-dates the owning real intervals (CSV-only rev_dates)
_PUBLISH / DAILY-AGGREGATION / PARITY-TOOLING INTEGRITY · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/publish_internal.R` 142, 282-321 (build_rev_intervals at src/rate_schema.R:92-93)
- **problem:** The published per-interval rate panel (actual/snapshots/valid_from=*/rates.parquet — the artifact tariff-model reads) silently omits every synthetic boundary mint (bnd_<date>) and scheduled activation (sched_*), and publishes the *owning* real revision's PRE-boundary rates across the post-boundary window. publish_internal() resolves rev_dates via load_revision_dates() (line 142), which reads config/revision_dates.csv ONLY. The bnd_*/sched_* revisions are appended to rev_dates purely in-memory inside build_gather.R (build_boundary_mints / build_scheduled_activations) and are NEVER persisted to the CSV (confirmed: build_boundary_mints at 00_build_timeseries.R:365-367 just bind_rows in memory; its own docstring says 'rev_dates is reloaded from the CSV, which carries no bnd_ rows'). publish_series_snapshots() globs ALL snapshot_*.rds (so revs INCLUDES the synthetic ids) but computes intervals with build_rev_intervals(revs, rev_dates, ...), whose inner `rev_dates %>% filter(revision %in% revs_built)` (rate_schema.R:92-93) keeps only revs present in the CSV. The synthetic ids are therefore dropped from `ints`, so (a) their snapshot_bnd_*.rds / snapshot_sched_*.rds are never written to the published parquet tree, and (b) the owning real revision's valid_until is computed to the next REAL revision-1 (un-shortened), so its parquet covers the post-boundary window at the wrong (pre-boundary) rates. The fail-loud worker guard cannot catch it: n = nrow(ints) is already the reduced count, so length(results)==n holds. The daily series does NOT have this bug because gather passes the augmented rev_dates into run_daily_series; only the publish layer was left reading the CSV. This is precisely in the parity gate's blind spot — the gate compares snapshot_*.rds and the daily CSVs, never the published per-interval parquet tree.
- **evidence:** Empirically confirmed against the on-disk published demo panel data/timeseries_split_demo/: partitions jump 2026-02-12 -> 2026-02-24 with NO valid_from=2026-02-20 partition, yet golden 52dab78 contains snapshot_bnd_2026-02-20.rds (the IEEPA-invalidation boundary mint; revision_dates.csv confirms 2026-02-20 is NOT a real revision — 2026_rev_3=2026-02-12, 2026_rev_4=2026-02-24). Code: publish_internal.R:142 `rev_dates <- load_revision_dates()`; publish_series_snapshots.R:289-291 `revs <- sub(...); ints <- build_rev_intervals(revs, rev_dates, as.Date(horizon_end))`; rate_schema.R:92-93 `rev_dates %>% filter(revision %in% revs_built)`.
- **suggested fix (NOT applied):** Thread the gather-augmented rev_dates into publish. Either (a) have build_gather.R persist the minted/scheduled {revision, effective_date} rows to a side file (e.g. data/timeseries/synthetic_revisions.rds or amend metadata.rds) and have publish_internal load+union them into rev_dates before calling publish_series_snapshots, or (b) have build_rev_intervals derive the missing synthetic effective_dates from the snapshot ids themselves (bnd_<date>/sched_<date> encode the date) instead of silently dropping any revs_built not found in rev_dates — and at minimum stop()/warn loudly when revs_built contains ids absent from rev_dates rather than dropping them. Add the published per-interval parquet panel to the parity harness so this class of regression is gated.
- **verifier:** Independently verified every load-bearing claim against the actual theseus code (not the finding's quoted snippets), plus master and on-disk artifacts.

MECHANISM (confirmed in code):
- /nfs/.../src/publish_internal.R:142 `rev_dates <- load_revision_dates()` — reads config/revision_dates.csv ONLY. Confirmed load_revision_dates (src/revisions.R:78) read_csv's the CSV with no in-memory augmentation.
- publish_series_snapshots (publish_internal.R:282-348) globs ALL snapshot_*.rds (line 285), derives revs from filenames (line 289, so synthetic bnd_/sched_ ids ARE included), then ints <- build_rev_

### [HIGH] Universal IEEPA Annex-II exempt list gained per-revision date-windowing vs master (1146 codes change exemption status), baked into re-frozen golden
_TIMING / DATE-GATING / CROSS-REVISION LEAKAGE · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/authority_adapter.R` 314-335 (adapter); compare git show master:src/06_calculate_rates.R:789-795
- **problem:** The adapter's .resolve_ieepa_exempt_products() filters the universal IEEPA Annex-II exempt list by effective_date_start (as.Date(start) <= rev_date) and effective_date_end (as.Date(end) >= rev_date) before baking it onto ieepa_reciprocal$programs[[1]]$exempt_products$universal. The pre-refactor master code did NOT do this: master loaded the entire list date-blind (`read_csv(ieepa_exempt_path, col_types=cols(hts10=col_character()))$hts10`). On theseus the CSV also newly carries effective_date_start/_end columns (merge-base 7779933 and master both have ONLY a `hts10` column). 1146 of 5052 rows carry a real window, so their IEEPA-reciprocal exemption status now flips by revision (e.g. copper ch74 codes with end=2025-07-31 become reciprocal-dutiable at rev_17+ when copper 232 begins; wood ch44 codes with end=2025-10-13 flip at rev_27+). This is a per-revision rate change for those product-country pairs vs the master baseline. The windowing logic itself is legally correct (the copper/wood end dates align with their 232 program starts), so this is a correctness IMPROVEMENT — but it is NOT bit-identical to master, was folded into the twice-re-frozen golden (70b6b97 -> 52dab78), and is therefore completely invisible to the candidate==golden parity gate. It is exactly the class of change the gate cannot see.
- **evidence:** adapter src/authority_adapter.R:324-333: `if ('effective_date_start' %in% names(ie_raw)) { ie_raw <- ie_raw |> dplyr::filter(is.na(effective_date_start) | as.Date(effective_date_start) <= rd_exempt) } ; if ('effective_date_end' %in% names(ie_raw)) { ie_raw <- ie_raw |> dplyr::filter(is.na(effective_date_end) | as.Date(effective_date_end) >= rd_exempt) }`. master:06:791: `read_csv(ieepa_exempt_path, col_types = cols(hts10 = col_character()))$hts10` (no window). `git show master:resources/ieepa_exempt_products.csv | head -1` => `hts10`; `git show theseus:resources/ieepa_exempt_products.csv | head -1` => `hts10,effective_date_start,effective_date_end`. The columns/filter arrived on theseus via commit eb145ba ('Port master fix4+fix5 ... date-window'); the source fixes c5b2eb1/38757f6 are NOT on master. awk over the CSV: 1146/5052 rows have a non-NA window; 113 copper rows end 2025-07-31, 419 wood rows end 2025-10-13. The calc comment at 06:966-970 itself admits 'Without the window, amendments applied retroactively (extreme-eta review item 3)' — confirming the windowing is new behavior, not a relocation.
- **suggested fix (NOT applied):** Keep the windowing (it is the legally-correct behavior) but stop calling it a parity-safe relocation. Document it as an intentional master-fix port that CHANGES output vs the pre-refactor master baseline, and validate it against master numerically (a known, expected delta on the windowed codes for revs 17-32) rather than only against the re-frozen golden. At minimum, correct the adapter docstring at lines 313-314 which claims 'VERBATIM from 06_calculate_rates.R (the old inline load)'.
- **verifier:** CONFIRMED with concrete evidence. I independently verified every load-bearing claim.

1. ADAPTER CODE (src/authority_adapter.R:314-335) does exactly what the finding says: `.resolve_ieepa_exempt_products()` reads the CSV then filters `is.na(effective_date_start) | as.Date(effective_date_start) <= rd_exempt` and `is.na(effective_date_end) | as.Date(effective_date_end) >= rd_exempt`. The quoted snippet matches the real lines.

2. MASTER BASELINE: `git show master:src/06_calculate_rates.R:791` loads the list date-blind: `read_csv(ieepa_exempt_path, col_types=cols(hts10=col_character()))$hts10` — 

### [HIGH] §232 (the largest relocation) has NO oracle test — new-calc-vs-old-calc equivalence rests only on parity vs the re-frozen golden
_TEST QUALITY / PARITY-BLINDNESS / COVERAGE GAPS · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `tests/test_authority_adapter.R, tests/test_ieepa_deblob.R` test_authority_adapter.R:123-194 (s232 structure checks); test_ieepa_deblob.R:45-100 (the oracle pattern that §232 lacks)
- **problem:** The IEEPA reciprocal, fentanyl, and the four product-exemption sets each got a faithful 'oracle' test: the test embeds the verbatim pre-relocation calc code and asserts the adapter reproduces it bit-for-bit (test_ieepa_deblob.R oracle_country_ieepa at lines 47-100 matches master 06_calculate_rates.R:852-972 exactly; test_exempt_sets_in_spec.R oracles the real CSV load+date-gate). But §232 — by far the biggest and most complex de-blob (Plank 4a default/by_country/overrides/floors + Plank 4c annex tier/flat_rate/floor_rate/country_overrides, the Russia-200%-override path, EU27 census expansion, ISO->census mapping) — has NO such oracle. test_authority_adapter only asserts the spec FIELDS are populated (e.g. .prog('steel')$rate$default == 0.50, by_country == {1220:0}) and that resolve_rate reads them back; it never runs the OLD §232 calc and asserts the new spec-driven §232 produces the same per-HTS×country rate_232. So the entire correctness of the §232 relocation is gated ONLY by the numeric parity gate vs a golden that was re-frozen twice after behavior-changing work — exactly the case the charter flags as silent-drift-prone. A §232 relocation that moved a number and got baked into 70b6b97/52dab78 would be invisible.
- **evidence:** grep for an oracle asserting new-232 == old-232 returns NONE. test_authority_adapter.R asserts only structure: line 126 `check(identical(.prog('steel')$rate$default, 0.50), ...)`, line 142 `check(identical(.prog('steel')$rate$by_country, stats::setNames(0, '1220')), ...)`. The calc reads annex off the spec at 06_calculate_rates.R:2070-2135 (ann$tier, ann$flat_rate, ann$floor_rate, ann$country_overrides rate_map/mode) with zero unit-test coverage of that read producing the old rate. test_authority_adapter uses census-ONLY stubs (is_232_exempt census-only, ISO_TO_CENSUS/EU27_CODES are stubs), so the real EU27 expansion and ISO-mapping path is never exercised by any test.
- **suggested fix (NOT applied):** Add tests/test_s232_deblob.R mirroring test_ieepa_deblob.R: embed the pre-Plank-4a/4c §232 rate-application code (git show 7779933:src/06_calculate_rates.R for the §232 block) as a verbatim oracle, run it on a realistic fixture covering steel/aluminum/copper base, exempt by_country, UK/EU auto+wood deals (ISO + EU27 expansion), and all annex tiers incl. a country_overrides mode='max' case, and assert the spec-driven calc reproduces rate_232 per HTS×country. This is the one large relocation whose 'bit-exact' claim is currently unverifiable except through the (blind) golden.
- **verifier:** VERIFIED against the actual code (not the finding's snippets). Both test files are new on theseus (absent on master), so this is theseus-introduced coverage.

The oracle asymmetry is real and exactly as described:
- IEEPA reciprocal: tests/test_ieepa_deblob.R:47-100 `oracle_country_ieepa` embeds the verbatim pre-relocation calc (phase-collapse + FLOOR_COUNTRIES surcharge->floor override, Swiss-window-gated) and asserts the adapter reconstructs country_ieepa bit-for-bit (same_table, lines 165/172).
- IEEPA fentanyl: lines 248-267 oracle the general max-per-census + carveouts.
- Exempt sets: tes

### [HIGH] Russia §232 surcharge leak: 3 absolute invariants RED while parity is GREEN — confirms the parity gate is blind to bugs baked into the golden
_TEST QUALITY / PARITY-BLINDNESS / COVERAGE GAPS · pre-existing · verdict=confirmed · finder-confidence=high_
- **file:** `tests/test_rate_calculation.R` 856-870, 872-894, 908-920
- **problem:** Three absolute invariants on snapshot_2026_rev_5.rds fail: (a) 'Russia annex_2 products are NOT surcharged' (856), (b) 'Russia steel (ch72/73) does NOT get the aluminum-only surcharge' (872), (c) 'Annex II × rate_232>0 is semi-products-only / Note 39a' (908). The 200% Russia surcharge, scoped to aluminum, leaks onto Russia steel and onto Annex-II non-semiconductor products. This bug is present in BOTH candidate and golden, so the parity gate is GREEN on those snapshots — only these absolute invariants catch it. This is the live demonstration of the charter's thesis: parity != correctness; a re-frozen golden can carry a wrong number forever. It is the country_overrides rate_map application (06_calculate_rates.R:2131-2135) whose annex/metal-type/chapter scoping is mis-baked by the adapter — and which, per finding #1, has no oracle test.
- **evidence:** Doc spec_driven_calculator_plan.md:610-616 and :600-601 admit '3 absolute invariants ... Present in the current golden data, so the parity gate is green on it — only the invariant tests catch it.' The override applies via `newr <- if (identical(ov$mode,'max')) pmax(rates$rate_232, unname(r)) else unname(r)` (06:2134) with rate_map scoping baked adapter-side. test_rate_calculation.R:893 asserts ru_steel rate_232 < 1.0 (fails), :869 asserts non_semi_a2 rate_232 == 0 (fails), :919 asserts leak nrow == 0 (fails).
- **suggested fix (NOT applied):** Pre-existing; fix the aluminum-surcharge scoping in the §232 annex country_overrides rate_map (restrict to aluminum metal_type / chapter 76 + aluminum derivatives, exclude steel ch72/73 and Annex-II non-semi). Then re-freeze the golden ONLY after the absolute invariants pass — and keep these invariants in the standing gate so the parity re-freeze can never re-bury the leak.
- **verifier:** Independently verified every load-bearing claim by reading the real code and executing the invariants against the actual snapshot.

(1) The three invariant tests exist exactly at the cited lines in /nfs/roberts/project/pi_nrs36/jar335/Repositories/tariff-rate-tracker/tests/test_rate_calculation.R: (a) lines 856-869 "Russia annex_2 products are NOT surcharged" asserting all(non_semi_a2$rate_232==0); (b) lines 872-893 "Russia steel (ch72/73) does NOT get the aluminum-only surcharge" asserting all(ru_steel$rate_232<1.0); (c) lines 908-919 "Annex II × rate_232>0 is semi-products-only (Note 39a)" a

### [MEDIUM] set_stacking counterfactual is silently dropped by the specs-less daily re-aggregation in 09
_Counterfactual scenario verbs · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/scenario_ops.R (verb) + src/09_daily_series.R (drop site) + src/stacking.R (fallback)` scenario_ops.R:361-375 (op_set_stacking); 09_daily_series.R:131,155,173,192,267,290 (apply_stacking_rules called with no stacking_policy); stacking.R:290 (policy <- stacking_policy %||% default_stacking_policy)
- **problem:** op_set_stacking is the ONLY verb whose entire effect lives in the stacking POLICY rather than in a per-authority rate_* column. In 06 the policy is honored: line 2976-2977 calls apply_stacking_rules(..., stacking_policy = stacking_policy_from_specs(specs, CTY_CHINA)), so the snapshot's total_additional/total_rate reflect the scenario's class/exceptions. But the daily aggregator re-derives total_additional/total_rate from the persisted, UNCHANGED rate_* columns: build_daily_aggregates -> compute_agg_overall/country/category all call apply_stacking_rules(rev_data, stacking_method = stacking_method) with NO stacking_policy, which falls back to default_stacking_policy() (stacking.R:290). The set_stacking mutation never reaches the published daily series; it reverts to baseline stacking with no error, warning, or message. Every OTHER verb survives this path because it changes a persisted rate_* column (rate_232, rate_ieepa_recip, rate_other, ...) and the default stacking re-derivation then gives the right total.
- **evidence:** 06 honors it: src/06_calculate_rates.R:2976 `rates <- apply_stacking_rules(rates, CTY_CHINA, stacking_method = stacking_method, stacking_policy = stacking_policy_from_specs(specs, CTY_CHINA))`. 09 drops it: src/09_daily_series.R:131 `rev_data <- apply_stacking_rules(rev_data, stacking_method = stacking_method)` (no stacking_policy arg; same at 155/173/192/267/290). Fallback: src/stacking.R:290 `policy <- stacking_policy %||% default_stacking_policy(cty_china)`. apply_stacking_rules RECOMPUTES total_additional/total_rate from rate_* (stacking.R:297-298), discarding 06's scenario totals. op_set_stacking's own docstring admits the boundary (scenario_ops.R:355-360) but the user-facing daily output is wrong, silently.
- **suggested fix (NOT applied):** Thread the scenario's stacking_policy (or the whole specs / a persisted policy descriptor) into build_daily_aggregates -> compute_agg_* -> apply_stacking_rules for the scenario path, OR persist 06's already-stacked total_additional/total_rate in the snapshot and have the daily aggregator read those columns instead of re-stacking when a non-default policy was used. At minimum, make op_set_stacking fail loud (or warn at apply_operations time) when the run will pass through the specs-less daily re-aggregation, so a counterfactual cannot silently no-op.
- **verifier:** VERIFIED REAL. I independently re-read all three cited files and traced the data flow.

MECHANISM CONFIRMED:
- op_set_stacking (src/scenario_ops.R:361-375) mutates ONLY specs[[auth]]$stacking via modifyList — it writes NO rate_* column (grep-confirmed). It is the only verb whose effect lives purely in the stacking policy, not in a persisted rate_* column.
- 06 honors it: src/06_calculate_rates.R:2976-2977 calls apply_stacking_rules(rates, CTY_CHINA, stacking_method=..., stacking_policy = stacking_policy_from_specs(specs, CTY_CHINA)), so the snapshot's total_additional/total_rate reflect the sc

### [HIGH] Russia 200% steel surcharge leaks onto 6 chapter-73 spring products (7320*) via the heading-program annex guard
_§232 de-blob parity · pre-existing · verdict=confirmed · finder-confidence=high_
- **file:** `config/policy_params.yaml + src/06_calculate_rates.R + src/authority_adapter.R` policy_params.yaml:270-274; 06_calculate_rates.R:2173 (heading guard); authority_adapter.R:122-127 (.s232_blanket_by_country config drain)
- **problem:** Russia (census 4621) chapter-73 steel spring products 7320103000/7320106015/7320106060/7320109015/7320109060/7320201000 carry rate_232 = 2.00 (200%) in annex_1a, instead of the annex_1a flat 0.50. The aluminum-only annex country_surcharge (policy_params.yaml:336-341, metal_types:['aluminum']) is correctly scoped and is NOT the source. The true root cause is a two-stage interaction: (1) section_232_country_exemptions has Russia rate:2.0 with applies_to:['steel','aluminum'] (line 273), so the PRE-annex steel by_country overlay (built by .s232_blanket_by_country draining S232_COUNTRY_EXEMPTIONS) sets Russia steel = 2.0 for all of ch72/73; (2) the annex override's first case arm `hts10 %in% heading_program_products ~ rate_232` (06:2173) PRESERVES that pre-annex 2.0 for any product that is also an auto/MHD heading part. 7320* springs are listed in resources/s232_auto_parts.txt (732010, 73202010) and s232_mhd_parts.txt (73201030/60/90, 73202010), so they fall into heading_program_products and never get re-priced to the annex_1a flat 0.50 — the 200% survives. Net effect: the aluminum-only Proc-10522 surcharge effectively lands on Russia steel springs, contradicting the config's metal_types:['aluminum'] annex scoping and the policy intent that under the April-2026 annex regime steel is priced at the annex tier.
- **evidence:** Ran rev_5 snapshot: Russia non-aluminum-derivative steel (annex 1a/1b/3) rate_232 distribution = {0.50: 1054 rows (annex_1a, correct), 0.25: 64 rows (annex_1b, correct), 2.00: 6 rows (annex_1a, LEAK)}; all 6 leaking HS10s start with 7320. For 7320103000: Russia=2.00, UK=0.25 (correct UK steel annex_1a deal), China/all-others=0 (heading-guard preserves their pre-annex 0). grep confirms 732010 in resources/s232_auto_parts.txt:16 and 73201030/60/90 in resources/s232_mhd_parts.txt:17-19. The identical 6-row/2.00 footprint appears in tests/golden/9258c93 (steel>=1.0 leak rows: 6; 7320103000@RU=2.000), proving it predates the spec refactor and is baked into the re-frozen golden — the numeric parity gate cannot see it. tests/test_rate_calculation.R:872-893 asserts Russia non-alum-deriv steel rate_232 < 1.0 and FAILS on these 6 rows.
- **suggested fix (NOT applied):** Resolve the policy inconsistency: either (a) change section_232_country_exemptions Russia entry to applies_to:['aluminum'] (policy_params.yaml:273) so the pre-annex blanket never puts 200% on Russia steel — matching the aluminum-only annex country_surcharge intent; or (b) in the annex override, do NOT let the heading-program guard preserve a pre-annex BLANKET metal rate for primary-chapter (72/73/76/74) products — restrict the `hts10 %in% heading_program_products ~ rate_232` arm to non-primary-chapter products, or re-apply the annex flat to primary-chapter heading products. Option (a) is the smaller, more defensible change. NOTE: this requires a deliberate golden re-freeze since the gate is blind to it.
- **verifier:** Every load-bearing claim in the finding is verified independently — by reading the real code AND by running the rev_5 snapshot.

CODE CHAIN (all confirmed by Read/grep, not the finding's quotes):
1. config/policy_params.yaml:271-274 — section_232_country_exemptions Russia (census 4621) entry: rate:2.0, applies_to:['steel','aluminum'], expiry_date:null. So steel is in scope of the 200% pre-annex blanket.
2. src/authority_adapter.R:122-127 — .s232_blanket_by_country loops over pp$S232_COUNTRY_EXEMPTIONS and, for `if (metal %in% ex$applies_to)`, sets bc[[cty]] <- ex$rate. On the steel metal pass 

### [MEDIUM] set_exempt on the autos program is a silent no-op: the calc no longer reads auto_exempt
_Counterfactual scenario verbs · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/scenario_ops.R` 494-502
- **problem:** op_set_exempt for program='autos' (and the other non-metal exempt programs in S232_EXEMPT_FIELD) writes the census codes onto the residual blob field r$auto_exempt (line 498). But the theseus refactor (Plank 4a/S3 close-out) deliberately REMOVED the calculator's read of auto_exempt, on the rationale that it was a 'verified no-op' in the baseline: autos flow through the heading path (heading_232_rate), which is keyed by hts10 only and has NO country dimension/exemption. So a counterfactual set_exempt(section_232, autos, {Japan}) writes a field the calculator ignores — the auto 232 tariff still applies to every in-scope country. The verb succeeds, validates, and the test passes (it only checks the blob field changed), yet the scenario has zero effect on calculated rates.
- **evidence:** scenario_ops.R:498 `r[[field]] <- as.character(countries)` (field='auto_exempt'). 06_calculate_rates.R:1748-1755 comment: 'the dead auto_base/auto_exempt/auto_rate plumbing was removed ... auto_exempt remains on the residual blob, still consumed by the independent extract_section232_rates() path in generate_etrs_config.R; only the calc's dead READ of it is dropped here.' The autos rate is set at 06_calculate_rates.R:1666-1671 (heading_product_rate, keyed by hts10 only, no country). On master (7779933) the calc DID read s232_rates$auto_exempt (06_calculate_rates.R:1489-1492) but applied it to country_232$auto_rate, which the same comment says NEVER set rate_232 — so even the master read was country-granular-dead for the heading-driven auto rate.
- **suggested fix (NOT applied):** Either (a) make op_set_exempt(autos) fail loud ('autos have no country-exemption mechanism in the heading path; not supported') so the no-op is visible, or (b) give the autos heading path a country-exemption join (a per-country mask on heading_232_rate for the autos headings) and have set_exempt write a structured field that join reads. Do NOT leave it writing auto_exempt silently. Mirror the steel/aluminum de-blobbed by_country-0 approach if autos should support country exemptions.
- **verifier:** I independently verified every factual claim by reading the real code (not the finding's snippet).

FACTS CONFIRMED:
1. scenario_ops.R is a NEW file on theseus — it does not exist in master or the 7779933 ancestor (`git show master:src/scenario_ops.R` -> "exists on disk, but not in master"). So there is no master `op_set_exempt` to compare against; the verb itself is wholly introduced by the refactor.
2. op_set_exempt for program='autos' (src/scenario_ops.R:494-502) takes the blob branch: line 498 `r[[field]] <- as.character(countries)` writes census codes onto the residual field auto_exempt. 

### [HIGH] set_stacking counterfactual is silently discarded by the specs-less daily re-aggregation (09)
_Counterfactual scenario verbs · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/scenario_ops.R` 343-375
- **problem:** op_set_stacking mutates a spec's stacking{class,exceptions}, which is load-bearing in the per-revision calc (06 reads it via stacking_policy_from_specs). But the published daily aggregates are NOT taken from the snapshot's total_rate/total_additional — build_daily_aggregates RE-DERIVES them by calling apply_stacking_rules(rev_data, stacking_method) and compute_net_authority_contributions(...) with stacking_policy = NULL, which falls back to default_stacking_policy(). Those functions recompute total_rate/total_additional/net_* from the persisted per-authority rate_* columns using the DEFAULT stacking, overwriting the scenario's stacking. So a set_stacking counterfactual (e.g. flip 301 to content_split) reaches the snapshot but is silently dropped from every daily output: overall, by-country, by-category, and by-authority. The doc (scenario_ops.R:355-360) is honest that this is a 'Pass-2 concern', but it remains a real correctness gap: a user running the verb gets a published panel identical to baseline stacking with no error.
- **evidence:** src/stacking.R:290 `policy <- stacking_policy %||% default_stacking_policy(cty_china)` inside apply_stacking_rules; lines 297-298 recompute total_additional/total_rate. 09_daily_series.R:131,155,173,192,267,290 call apply_stacking_rules(rev_data, stacking_method = stacking_method) with no stacking_policy; L221/L244 call compute_net_authority_contributions(..., stacking_method) with no stacking_policy. compute_net_authority_contributions default stacking_policy=NULL (stacking.R:325). No spec/policy is threaded into run_daily_series/build_daily_aggregates.
- **suggested fix (NOT applied):** Thread the scenario's stacking policy into the daily path: either persist the per-revision stacking_policy alongside each snapshot and have build_daily_aggregates pass it to apply_stacking_rules/compute_net_authority_contributions, OR have the daily step trust the snapshot's already-stacked total_rate/total_additional instead of re-deriving. Until fixed, op_set_stacking should at minimum emit a loud warning that the daily aggregate will not reflect it.
- **verifier:** Independently verified against the real code (scenario_ops.R is new on theseus; not on master, so this is a refactor-introduced gap). Every load-bearing claim checks out:

1. op_set_stacking (src/scenario_ops.R:361-375) mutates specs[[auth]]$stacking via modifyList — confirmed; it changes only the stacking {class, exceptions}, not the per-authority rate_* columns.

2. It IS load-bearing in the per-revision calc: 06_calculate_rates.R:3113-3114 calls apply_stacking_rules(..., stacking_policy = stacking_policy_from_specs(specs, CTY_CHINA)), so the persisted snapshot's total_rate/total_additional 

### [HIGH] test_alt_runner_equivalence.R is RED on theseus — stub signature drifted from .run_one_alt (operations arg)
_TEST QUALITY / PARITY-BLINDNESS / COVERAGE GAPS · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `tests/test_alt_runner_equivalence.R` 64-86
- **problem:** The serial-path unit test fails outright. .run_one_alt() (src/parallel.R:421-429) now calls build_alternative_timeseries(..., operations = spec$operations), an argument added on theseus (commit d51e047 'Phase 6e: the 09 rebuild path honors scenario ops'). The test's stub build_alternative_timeseries (lines 64-65) still has the master-era 4-param signature `function(pp_override, variant, imports = NULL, policy_params = NULL)` with no `operations` param. So every spec (a_ok, b_ok, fail_me) errors with 'unused argument (operations = spec$operations)' inside .run_one_alt's tryCatch, yielding status='failed' for ALL three. The assertion identical(statuses, c('ok','ok','failed')) then fails and the script exits 1. This is a live code path: alt_runner() is called in the real build at src/09_daily_series.R:1588 (default alt_workers=1 = the serial inproc path this test exercises), so the unit guard for alt_runner's per-spec failure-isolation provides ZERO coverage right now and would mask a real regression.
- **evidence:** Ran `Rscript tests/test_alt_runner_equivalence.R`:
  FAILED (a_ok): unused argument (operations = spec$operations)
  FAILED (b_ok): unused argument (operations = spec$operations)
  FAILED (fail_me): unused argument (operations = spec$operations)
  Error: identical(statuses, c("ok", "ok", "failed")) is not TRUE
Exit code 1. Confirmed via `git show 7779933:src/parallel.R` (master call had only `imports`/`policy_params`, matching the stub) vs current src/parallel.R:427 (`operations = spec$operations`). The real fn src/09_daily_series.R:1288-1295 DOES accept `operations = NULL`, so production is consistent — only the test stub is stale.
- **suggested fix (NOT applied):** Add `operations = NULL` (and ideally `snapshot_out_dir = NULL`, `allow_partial = FALSE` to match the full signature) to the test's stub build_alternative_timeseries on lines 64-65 so it absorbs the args .run_one_alt now passes. Then the synthetic-failure isolation (a_ok/b_ok ok, fail_me failed) is exercised again.
- **verifier:** Independently verified every claim. (1) I ran `Rscript tests/test_alt_runner_equivalence.R`: it is RED — exit code 1, all three specs print "FAILED (...): unused argument (operations = spec$operations)" and the assertion `identical(statuses, c("ok","ok","failed"))` fails. (2) The current stub at tests/test_alt_runner_equivalence.R:64-65 is exactly the master-era 4-param signature `function(pp_override, variant, imports = NULL, policy_params = NULL)` with no `operations` param (grep for "operations" in the test returns nothing). (3) Current src/parallel.R:421-429 (.run_one_alt) calls `build_alt

### [HIGH] Russia §232 aluminium-surcharge leak: 3 absolute invariants fail, baked into the golden — parity is green on a real correctness bug
_TEST QUALITY / PARITY-BLINDNESS / COVERAGE GAPS · pre-existing · verdict=confirmed · finder-confidence=high_
- **file:** `tests/test_rate_calculation.R` 856-869, 872-894, 908-920
- **problem:** The 200% Russia surcharge (legally scoped to ALUMINUM) leaks onto (a) Russia steel HS 72/73 and (b) Annex-II non-semiconductor products that should be §232-exempt (Note 39a). Three absolute invariants assert this can't happen, and they FAIL on snapshot_2026_rev_5.rds. The leak is present identically in the frozen golden, so the numeric parity gate is GREEN on it — this is the live, concrete demonstration that 'parity GREEN 47/47' does not mean correct. The de-blob (Plank 4c) faithfully reproduced master's surcharge-scoping behavior, so the bug was carried over, not introduced — but it remains unfixed on theseus and the only thing catching it is these 3 data-gated invariants (which themselves silently skip without the snapshot, per the previous finding).
- **evidence:** docs/spec_driven_calculator_plan.md:610-616 'tests/test_rate_calculation.R fails 3 absolute invariants (lines ~856/872/908) ... Present in the current golden data, so the parity gate is green on it — only the invariant tests catch it.' The invariants: line 869 stopifnot(all(non_semi_a2$rate_232 == 0)); line 893 stopifnot(all(ru_steel$rate_232 < 1.0)); line 919 stopifnot(nrow(leak) == 0). git show 7779933:src/06_calculate_rates.R confirms the country-surcharge application logic the adapter now bakes into ann$country_overrides (mode='max') is the same scoping that leaks.
- **suggested fix (NOT applied):** Fix the surcharge scoping so the aluminum 200% rate_map only matches aluminum-classified products (correct the adapter's tier+metal-type+chapter scoping in the country_overrides rate_map build, authority_adapter.R ~609-650), then re-freeze the golden. Until fixed, keep the 3 invariants as the standing guard and ensure they run in CI (see the silent-skip finding). This is a behavior-changing fix (own task), but it is a real, shipping correctness bug, not just a test artifact.
- **verifier:** I independently verified every load-bearing claim. (1) The 3 invariants exist verbatim in tests/test_rate_calculation.R: line 869 stopifnot(all(non_semi_a2$rate_232 == 0)), line 893 stopifnot(all(ru_steel$rate_232 < 1.0)), line 919 stopifnot(nrow(leak) == 0) — confirmed by Read. (2) They are present identically on master at the same line numbers (git show master:...), so introduced_by_refactor: false is correct. (3) I ran all three against the live data/timeseries/snapshot_2026_rev_5.rds (module load R/4.4.2-gfbf-2024a) and ALL THREE FAIL: invariant 1 = 133 nonzero annex_2 non-semi rows; invar

### [MEDIUM] Oracle test and adapter docstring falsely claim the IEEPA-exempt windowing is the 'EXACT pre-relocation calc' / 'VERBATIM' code
_TIMING / DATE-GATING / CROSS-REVISION LEAKAGE · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `tests/test_exempt_sets_in_spec.R` 9-13, 52-71 (test); src/authority_adapter.R:313-314, 297-309 (docstrings)
- **problem:** The Pass-1.5 relocation is asserted to be 'bit-exact by construction' and tested by an oracle that is 'the ORIGINAL calc load+date-gate code (copied verbatim below as the ORACLE)'. But the oracle_ieepa_exempt() function (test lines 52-71) itself contains the effective_date_start/_end windowing — it is a copy of the NEW theseus behavior, not the master original (which had no windowing, master:06:791). So the test proves only that the adapter equals a hand-written copy of itself; it cannot and does not catch the master->theseus divergence documented in the finding above. This is the parity-gate blind spot reproduced at the unit-test layer: the 'oracle' was re-baselined to the post-change behavior, so it is green by construction. The adapter docstring at 313-314 repeats the false 'VERBATIM from 06_calculate_rates.R (the old inline load)' claim.
- **evidence:** tests/test_exempt_sets_in_spec.R:9-11 header: 'This test runs the ORIGINAL calc load+date-gate code (copied verbatim below as the ORACLE) against the real resource CSVs and asserts the adapter helpers reproduce it bit-for-bit'. Oracle body lines 59-67 contain `filter(is.na(effective_date_start) | as.Date(effective_date_start) <= rd_exempt)` and the matching end filter — neither present in the real pre-relocation master code (06:789-795 reads `$hts10` with no filter). The country_eo and s122 oracles in the same file ARE faithful to master; only the ieepa_exempt oracle is mis-described.
- **suggested fix (NOT applied):** Either (a) restore the oracle to master's actual pre-refactor code (`read_csv(...)$hts10`, no window) so the test surfaces the windowing as an intentional, documented delta, or (b) relabel the test and the adapter docstring to state plainly that this is a NEW date-window (ported master fix), not a verbatim relocation, and that the unit test is a self-consistency check, not a master-parity check.
- **verifier:** CONFIRMED on every load-bearing claim, verified independently of the finding's quoted snippets.

(1) The file does not exist on master (`git show master:tests/test_exempt_sets_in_spec.R` -> "exists on disk, but not in master"), so it is a new theseus test.

(2) Master's actual pre-relocation inline load (git show master:src/06_calculate_rates.R line 791) is: `read_csv(ieepa_exempt_path, col_types = cols(hts10 = col_character()))$hts10` — NO effective_date_start/_end windowing whatsoever. The finding's master citation (06:789-795, reads $hts10 with no filter) is exactly right.

(3) The test ora

### [MEDIUM] set_stacking counterfactual is invisible to the daily (09) re-aggregation — documented Pass-2 gap
_dead-code / vestigial / completeness / missing-things · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/09_daily_series.R` 131,155,173,192,267,290
- **problem:** op_set_stacking (scenario_ops.R, a NEW theseus verb) mutates a spec's stacking{class,exceptions}, which 06_calculate_rates.R reads via stacking_policy_from_specs() when it recomputes a snapshot. But the daily aggregation path in 09 RE-STACKS the persisted rate_* columns with apply_stacking_rules(..., stacking_method=stacking_method) and passes NO stacking_policy, so apply_stacking_rules falls back to default_stacking_policy(cty_china) (stacking.R:290). A set_stacking scenario therefore lands in the per-snapshot 06 output but is silently dropped by any daily re-aggregation that bypasses 06 — the daily_overall/by_country/by_category numbers do NOT reflect the re-routed stacking class. This is a genuine completeness gap for the set_stacking verb's stated purpose (e.g. flipping section_301 to content_split).
- **evidence:** 09_daily_series.R:131 `rev_data <- apply_stacking_rules(rev_data, stacking_method = stacking_method)` and identical specs-less calls at 155/173/192/267/290; stacking.R:290 `policy <- stacking_policy %||% default_stacking_policy(cty_china)`. scenario_ops.R:355-360 SCOPE NOTE explicitly acknowledges: 'the 09 daily aggregate RE-STACK reads the PERSISTED rate_* columns with NO specs ... so a set_stacking counterfactual is NOT seen by a daily re-aggregation that bypasses 06 ... a Pass-2 concern'.
- **suggested fix (NOT applied):** Either thread the scenario's stacking_policy (stacking_policy_from_specs of the mutated specs) into the daily aggregation's apply_stacking_rules calls, or persist the resolved stacking policy alongside each snapshot and have 09 load+pass it. At minimum, make build_alternative_timeseries refuse / loudly warn when a scenario's operations include set_stacking, so a user cannot silently get baseline-stacked daily output for a stacking counterfactual.
- **verifier:** VERIFIED against the real code, not the finding's snippet.

(1) Cited lines accurate. /nfs/roberts/project/pi_nrs36/jar335/Repositories/tariff-rate-tracker/src/09_daily_series.R lines 131, 155, 173, 192, 267, 290 all call apply_stacking_rules(rev_data/wt_data, stacking_method = stacking_method) with NO stacking_policy arg. The signature (src/stacking.R:252) is apply_stacking_rules(df, cty_china='5700', stacking_method='mutual_exclusion', stacking_policy=NULL), and src/stacking.R:290 does `policy <- stacking_policy %||% default_stacking_policy(cty_china)`. So all six 09 calls resolve to the def

### [MEDIUM] test_timeline_invariants — the sole absolute guard for the P2-1 mints — is data-dependent and effectively does not run; in this environment it timed out before emitting a single assertion
_TEST QUALITY / PARITY-BLINDNESS / COVERAGE GAPS · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `tests/test_timeline_invariants.R` 44-49 (skip_all gates), 77-85 (snap_on reads full ~1.3GB snapshots)
- **problem:** This is billed (header lines 5-7) as the ABSOLUTE-assertion test that catches what parity-vs-old-golden cannot, because the mints change the intervals they touch. But it only runs if data/timeseries holds both real snapshot_*.rds AND the bnd_ snapshots, and each snap_on() readRDS pulls a multi-GB snapshot. On the shared login/interactive node it could not complete: my run hit the 500s timeout (exit 144) without printing any 'ok:' line. A test that only passes under a dedicated high-mem Slurm job — and is skip_all'd whenever the build artifacts are absent — is not part of the routine gate. The doc claims 'test_timeline_invariants 17/17 green' but that green came from one manual run against one build; it is not a standing regression guard the way the pure-logic suites are.
- **evidence:** Header line 8-9: 'SKIPS cleanly when the build artifacts ... are absent.' Lines 47-49 skip_all when no bnd_ snapshots. My execution: background task b7ahvsfdd 'failed with exit code 144' (timeout) after producing zero assertion output. The .NEEDED column-subset + gc() at lines 74-85 is a memory mitigation, confirming each readRDS is heavyweight.
- **suggested fix (NOT applied):** Persist a tiny fixture: write a stripped (hts10/country/rate_* only) parquet/rds for just the dates the assertions query (2025-03-11/12, 2026-02-19/20/23/24, 2026-11-09/10) and point the test at that, so the absolute invariants run in seconds in the normal environment. As-is, this critical guard runs essentially never.
- **verifier:** I independently verified every load-bearing claim against the real code, the git history, and a live measurement; the finding holds.

CODE (read directly, /nfs/roberts/project/pi_nrs36/jar335/Repositories/tariff-rate-tracker/tests/test_timeline_invariants.R): It is a NEW file (git diff 7779933...theseus: 165 insertions, added in commit 1f07a3e; absent on master). Lines 46-49 skip_all when no snapshot_*.rds or no bnd_ snapshots. Lines 77-85: snap_on() does full readRDS of a snapshot, subsets to .NEEDED rate/key columns, rm()+gc() — an explicit memory mitigation, exactly as quoted. Header lines 

### [MEDIUM] 09 daily per-authority re-stack is specs-less: a set_stacking counterfactual gets a wrong authority decomposition and net_* that no longer sum to total_additional
_Stacking generalization · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/09_daily_series.R` 220-221, 232, 243-247
- **problem:** build_daily_aggregates() calls compute_net_authority_contributions(rev_data, cty_china = CTY_CHINA, stacking_method = stacking_method) with NO stacking_policy argument, so it always falls back to default_stacking_policy() (stacking.R:344). The headline ETR (mean_total_exposed / weighted_etr) is read from the PERSISTED rev_data$total_additional/total_rate, which 06 computed with the spec-driven stacking_policy_from_specs() — so those are correct. But the per-authority decomposition (mean_232, mean_301, mean_ieepa, ...) and the by_authority breakdown are RE-DERIVED here from the wide rate_* columns using the DEFAULT policy. Under a set_stacking counterfactual (e.g. flip section_301 to content_split, or edit fentanyl's per-country exceptions) the persisted totals reflect the mutation but the daily decomposition does not, so (a) the per-authority means are wrong and (b) the net_* contributions no longer sum to total_additional. The daily workbook README explicitly asserts the opposite: 'Net authority contributions sum to total_additional (after stacking and mutual exclusion rules)' (09_daily_series.R:782).
- **evidence:** 09_daily_series.R:220 `net_data <- compute_net_authority_contributions(rev_data, cty_china = CTY_CHINA, stacking_method = stacking_method)` (no stacking_policy=). stacking.R:344 `policy <- stacking_policy %||% default_stacking_policy(cty_china)`. The README invariant: 09_daily_series.R:782 `c('Net authority contributions sum to total_additional (after stacking and mutual exclusion rules).', '')`. The team documents the exact boundary in scenario_ops.R:355-360: 'The 09 daily aggregate RE-STACK reads the PERSISTED rate_* columns with NO specs (it falls back to default_stacking_policy), so a set_stacking counterfactual is NOT seen by a daily re-aggregation.'
- **suggested fix (NOT applied):** Thread the spec-derived stacking policy into the daily re-stack (or persist net_* / the policy with each snapshot). Concretely: have build_alternative_timeseries() carry the policy (or the specs) alongside the snapshot, and pass stacking_policy = stacking_policy_from_specs(specs, CTY_CHINA) into both compute_net_authority_contributions() calls (lines 220 and 243). At minimum, since net_* already sum to total_additional only under the default policy, persist net_* in the snapshot at resolution time (06) and have 09 read them instead of re-deriving, so the decomposition can never disagree with the persisted total.
- **verifier:** Independently verified against the real code (not the finding's quotes). The cited lines do exactly what is claimed, and the divergence is genuine, reachable, and introduced by the refactor.

CITED CODE IS REAL: src/09_daily_series.R:220-221 and 243-244 call compute_net_authority_contributions(rev_data/wt_data, cty_china = CTY_CHINA, stacking_method = stacking_method) with NO stacking_policy= argument. In stacking.R:344 (mutual_exclusion branch of compute_net_authority_contributions) this falls back to `policy <- stacking_policy %||% default_stacking_policy(cty_china)`. The README invariant at

### [MEDIUM] Non-ad-valorem (compound/specific) duty leak: base_rate NA -> silently coalesced to 0; fix1 widened the affected set by design
_Master-fix port + revision re-dating · pre-existing · verdict=confirmed · finder-confidence=high_
- **file:** `src/helpers.R` 97-98 (parse_rate); compounded at src/04_parse_products.R:74-129 and the coalesce sites src/06_calculate_rates.R:178,802,1192,1761,2379,2449
- **problem:** parse_rate() returns NA for any compound or specific (non-ad-valorem) duty, and every downstream consumer does coalesce(base_rate, 0). So a tariff line with a real specific/compound MFN duty is treated as statutory-zero. This is pre-existing (helpers.R parse_rate/is_simple_rate are unchanged by theseus). fix1 (keeping 8-digit leaf lines) deliberately ENLARGES the affected surface: the +473 newly-admitted ch91 watch / ch98 special-provision leaves are predominantly compound-duty, so they now enter the universe at base_rate=NA -> 0. The fix1 commit message acknowledges this ('Compound-duty leaves keep base_rate = NA -> coalesced to 0 downstream'). The parity gate cannot catch this because both candidate and golden share the same NA->0 behavior, and the re-frozen golden baked in the enlarged set.
- **evidence:** src/helpers.R:97 '# Compound or specific rates - return NA (need manual handling)' then 'return(NA_real_)'. src/04_parse_products.R post-pass admits 8-digit leaves; cbe646d's own message: 'Compound-duty leaves keep base_rate = NA -> coalesced to 0 downstream'. No reconstruction of specific duties exists anywhere in the build.
- **suggested fix (NOT applied):** Track the magnitude: emit a per-revision count/share of admitted lines with NA base_rate that are non-ad-valorem (the parser already prints 'With NA base_rate'). Longer term, parse specific/compound duties to an ad-valorem-equivalent (needs unit-value data) or at minimum tag these cells so the statutory wedge is measurable rather than silently zeroed. No regression vs master, but the leak is now larger and undocumented in the panel quality report.
- **verifier:** Verified independently; every checkable claim holds.

CODE BEHAVIOR (confirmed): src/helpers.R:97-98 `parse_rate()` returns NA_real_ for any compound/specific (non-ad-valorem) duty; `is_simple_rate()` (105-111) accepts only "free" or ^[0-9.]+%$. All six downstream consumers in src/06_calculate_rates.R do `coalesce(base_rate, 0)` at exactly the cited lines 178, 802, 1192, 1761, 2379, 2449. So a line carrying a real specific/compound MFN duty becomes a statutory-zero cell. The compound string e.g. "6.4% + 3.5c/kg" fails both regexes and falls through to return(NA_real_) — deterministic by inspec

### [MEDIUM] set_active is largely inert: calc reads only ieepa_reciprocal AUTHORITY-level active.until; program-level / non-reciprocal / `from` writes are silent no-ops
_Counterfactual scenario verbs · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/scenario_ops.R` scenario_ops.R:329-341 (op_set_active); read sites: 06_calculate_rates.R:918; 00_build_timeseries.R:749-751
- **problem:** op_set_active writes $active at the authority level (no program) or programs[[pos]]$active (with program), for ANY authority. But the calculator reads exactly one active field: ieepa_invalidation <- specs[['ieepa_reciprocal']]$active$until (06:918), used to zero BOTH reciprocal and fentanyl. Consequences: (1) set_active with a `program` on ieepa_reciprocal writes programs[[1]]$active$until, which the calc never reads -> no-op for invalidation; (2) set_active on ieepa_fentanyl / section_301 / section_201 / section_232 / section_122 (any non-reciprocal authority) is read NOWHERE in 06 -> no-op; (3) the `from` field is never read by 06 at all (only `until` for reciprocal). The docstring (scenario_ops.R:329-330) claims set_active is 'Read by the calc for IEEPA invalidation (active.until)' and that 'from/until may be NA', overstating its reach. Additionally, the timeline splitter (timeline.R:74-80,221) WOULD read s$active$from/until to mint a boundary, but discover_boundaries is invoked from 00 with specs=NULL and no operations (00_build_timeseries.R:749-751), so a scenario set_active never creates its own interval-split boundary. The only fully-working case is authority-level set_active(ieepa_reciprocal, active=list(until=D)).
- **evidence:** src/06_calculate_rates.R:918 `ieepa_invalidation <- specs[['ieepa_reciprocal']]$active$until` is the SOLE $active read in 06 (grep for active$until/$active confirms). op_set_active program path: scenario_ops.R:337-338 writes programs[[pos]]$active (never read). discover_boundaries call: 00_build_timeseries.R:749 `discover_boundaries(rev_dates, output_dir, pp_build, overrides=..., horizon=...)` -- no specs/operations argument, so timeline.R:221 collect_schedule_boundaries(..., specs=NULL) and the s$active loop (timeline.R:74-80) never see scenario mutations. The existing unit test only covers program-level set_active on section_301 (test_scenario_ops.R:54-59), which is never read by the calc, so the test cannot detect the no-op.
- **suggested fix (NOT applied):** Either (a) restrict set_active to the one supported case (authority-level ieepa_reciprocal active.until) and fail-loud on program-level / non-reciprocal / `from`-only writes so a user cannot silently no-op, or (b) make the calc honor more active windows and thread the scenario-mutated specs into discover_boundaries so a set_active mints its own timeline boundary. Update the docstring to state the actual reach.
- **verifier:** Independently verified every load-bearing claim against the actual code (scenario_ops.R is new on theseus; master has no such file, so introduced_by_refactor:true is correct and there is no baseline parity-drift risk — empty ops = no-op).

(1) Sole $active read in the calc: grep across src/ shows exactly one spec-$active read in 06 — /nfs/.../src/06_calculate_rates.R:918 `ieepa_invalidation <- specs[['ieepa_reciprocal']]$active$until`, the AUTHORITY-level field. All other "active" mentions in 06 are Ch99-derived activation / heading gates / exempt_active, none reading spec$active.

(2) Program

### [LOW] set_exempt is semantically asymmetric: REPLACE for autos but additive-MERGE for steel/aluminum, contradicting its own 'replace the exemption list' docstring
_Counterfactual scenario verbs · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/scenario_ops.R` scenario_ops.R:459-498 (op_set_exempt); 474-487 (steel/aluminum merge) vs 489-494 (autos replace)
- **problem:** The function header (scenario_ops.R:459) says set_exempt 'replace[s] a section_232 program's country exemption list'. The autos branch honors that: r[[field]] <- as.character(countries) is a full REPLACE (line 493). But the steel/aluminum branch is an additive MERGE: it adds 0-entries for the requested countries over the existing by_country overlay, keeping every prior entry (line 484: c(existing[setdiff(names(existing), names(add))], add)). So for steel/aluminum the verb can only ADD exemptions, never set the list to exactly {countries} and never REMOVE a previously-exempt country. A user calling set_exempt(steel, countries=c('X')) expecting the exempt list to become {X} instead gets {X} added to all baseline steel exemptions (e.g. the HTS-override entries the adapter baked in). The two programs thus behave differently for identical-looking calls, and neither steel/aluminum call matches the documented 'replace' semantics.
- **evidence:** Probe confirmed (PROBE 9 logic, verified by reading): with baseline steel rate$by_country=c('1220'=0,'9000'=0.10), set_exempt(steel, countries='2010') yields by_country = {'1220'=0, '9000'=0.10, '2010'=0} -- the non-exempt HTS override '9000'=0.10 survives and the set is grown, not replaced. Autos: scenario_ops.R:493 `r[[field]] <- as.character(countries)` overwrites the whole list. Inline comment at scenario_ops.R:476-478 even says 'set_exempt only adds exempt-0s', directly contradicting the 'replace' docstring at line 459. The unit test (test_scenario_ops.R:172-177) only exercises the empty-baseline case where merge and replace coincide, so it cannot catch the asymmetry.
- **suggested fix (NOT applied):** Pick one semantic and make both branches match the docstring. If 'replace' is intended, the steel/aluminum branch should drop prior exempt-0 entries (or the whole by_country overlay) before writing the new set; if additive is intended, rename/clarify the verb and make autos additive too. At minimum fix the docstring to describe the per-program behavior accurately.
- **verifier:** I independently verified every technical claim against the real code and they all hold.

ASYMMETRY IS REAL. src/scenario_ops.R:459 docstring and the user-facing docs/scenarios.md:24 both say set_exempt "replace[s] a program's country exemption list" for steel/aluminum/autos alike. But the branches behave differently:
- Autos (line 493): `r[[field]] <- as.character(countries)` is a full REPLACE.
- Steel/aluminum (line 484): `c(existing[setdiff(names(existing), names(add))], add)` is an additive MERGE — it keeps every prior by_country entry whose name doesn't collide with the new countries, then

### [MEDIUM] Annex II 'Note 39a semi-products-only' invariant is violated by 31,746 auto/MHD heading rows (intentional heading preservation vs over-strict test)
_§232 de-blob parity · pre-existing · verdict=confirmed · finder-confidence=high_
- **file:** `src/06_calculate_rates.R + tests/test_rate_calculation.R` 06_calculate_rates.R:2168-2174 (heading_program_products guard); tests/test_rate_calculation.R:908-919 (invariant)
- **problem:** tests/test_rate_calculation.R:908-919 asserts that every annex_2 product with rate_232 > 0 must be a semi-product (Note 39(a) invariant). The actual rev_5 build has 31,746 annex_2 rows with rate_232 > 0, entirely in chapters 87 (8703 passenger cars: 16,080 rows; 8704 light trucks: 8,946) and 84 (8407 engines: 6,720), carrying their auto/MHD heading-program rates (0.2376, 0.25, 0.15, etc.) — NOT the semi 25%. This is by design: the annex override's first arm (06:2173) deliberately preserves heading-program rates so the annex_2 'removed from scope' catch-all does not wipe EU/JP/KR passenger-car floors and MHD blankets (see the load-bearing comment at 06:2156-2167). The 'semi-products-only' invariant the test encodes is therefore stricter than the code has ever satisfied — the code's real invariant is 'annex_2 rate>0 is semi-products OR heading-program products'.
- **evidence:** rev_5 snapshot: annex_2 rate>0 = 31,746 rows; chapter distribution {87: 25,026; 84: 6,720}; HS4 {8703:16,080, 8704:8,946, 8407:6,720}. The identical 31,746 count appears in tests/golden/9258c93, confirming pre-existing and gate-invisible. The leaking rate values (0.2376, 0.25, 0.15) are auto/MHD heading rates, not the 200% surcharge — so this is NOT the Russia surcharge leaking; it is heading-program preservation.
- **suggested fix (NOT applied):** Reconcile test and code intent. If heading-program preservation is correct policy (it is — auto/MHD are separate 232 authorities unaffected by the metals annex), amend the test at 908-919 to also exclude heading-program products (auto/MHD/copper/wood/semi), matching the code's real invariant. If the policy intent is that annex_2 truly zeroes everything, the heading guard is wrong — but that contradicts the documented EU-passenger-car rationale at 06:2156-2167, so amending the test is the right call.
- **verifier:** CONFIRMED by direct reproduction. I verified every element independently.

CODE (src/06_calculate_rates.R:2168-2177): The annex override's first case_when arm `hts10 %in% heading_program_products ~ rate_232` ("heading rate wins") deliberately preserves heading-program rates (auto/mhd/copper/wood/semi) ahead of the annex_2 catch-all, documented by the load-bearing comment at 2156-2167 (EU passenger cars under 9903.94.51 must keep ~12.5pp; the annex_2 catch-all `8703,2` was zeroing rate_232). This is by design — auto/MHD §232 are separate authorities unaffected by the metals annex.

TEST (tests/

### [MEDIUM] scenarios.yaml floor-patch capability (eu_auto_25pct) has no equivalent verb in scenario_ops.R — set_floor is deferred
_dead-code / vestigial / completeness / missing-things · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/scenario_ops.R` 40-44, 155-167
- **problem:** The deleted config/scenarios.yaml provided a floor-patch scenario (eu_auto_25pct: action type=floor, column=rate_232, value=0.25, filtered by country_group x product_set x from_date). The new operations engine has set_country_scope/set_active/disable/set_rate/set_exempt/set_stacking/add_program but NO floor verb: 'set_floor is a deferred follow-up' and apply_operation() errors loudly on it. So the as-of-date, country-group x product-set floor counterfactual that scenarios.yaml could express is not portable to the new engine. It is a documented deferral and fails loud (not a silent no-op), so no crash — but it is a real capability gap relative to what was deleted.
- **evidence:** scenario_ops.R:41 '# * set_floor (IEEPA universal baseline / phase-1 floor rate): a deferred follow-up.' and the switch() default at 163-166 ends with 'set_floor is a deferred follow-up.' Deleted scenarios.yaml (git show 7779933:config/scenarios.yaml) lines 31-39 define eu_auto_25pct with patches->action type:floor on rate_232.
- **suggested fix (NOT applied):** Either implement an op_set_floor verb (the rate$floors layer the §232 deals already use is the natural home, and the calc's pmax(floor-base,0) math exists), or document explicitly in docs/spec_driven_calculator_plan.md that the scenarios.yaml floor-patch use case is intentionally dropped and not regression-covered.
- **verifier:** Independently verified against the real code — every claim holds.

DELETED CAPABILITY IS REAL: `git show 7779933:config/scenarios.yaml` defines `eu_auto_25pct` with `action: {type: floor, column: rate_232, value: 0.25}`, filtered by country_group=eu x product_set=auto_vehicles x from_date='2026-05-04'. It had a functional consumer: `src/apply_scenarios.R` (420 lines) implemented `apply_patch()` doing the floor as `rate := max(value - base_rate, 0)` with the full filter+action vocabulary. `git diff 7779933...theseus` confirms both files deleted (459 deletions). The replacement `src/scenario_ops

### [MEDIUM] set_stacking operation is honored in 06 but silently ignored by the 09 daily re-stack (specs-less)
_dead-code / vestigial / completeness / missing-things · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/scenario_ops.R` 355-375 (op_set_stacking); src/09_daily_series.R:131,155,173,192,267,290
- **problem:** op_set_stacking is advertised as a SUPPORTED verb that 're-routes how an authority stacks'. It flows into calculate_rates_for_revision (06) via stacking_policy_from_specs, so the per-revision snapshot's rate_* columns reflect the new class. BUT the 09 daily aggregate re-runs apply_stacking_rules(rev_data, stacking_method=stacking_method) with NO specs and a fixed stacking_method, recomputing total_rate/total_additional from the persisted per-authority columns under the default stacking policy. Any daily aggregation that bypasses 06 therefore discards a set_stacking counterfactual and publishes a total_rate inconsistent with the snapshot's own per-authority columns. The author flags this honestly as a Pass-2 concern in the docstring, but the verb is still presented as supported, so a set_stacking scenario can produce a silently-wrong published total. The numeric parity gate cannot catch it (baseline uses no set_stacking).
- **evidence:** scenario_ops.R:357-360 'the 09 daily aggregate RE-STACK reads the PERSISTED rate_* columns with NO specs (it falls back to default_stacking_policy), so a set_stacking counterfactual is NOT seen by a daily re-aggregation that bypasses 06.' Confirmed at 09_daily_series.R:131 'rev_data <- apply_stacking_rules(rev_data, stacking_method = stacking_method)' (no specs threaded).
- **suggested fix (NOT applied):** Either thread the spec/stacking policy (or persist it with each snapshot) into the 09 re-stack so set_stacking is honored end-to-end, or downgrade set_stacking to error loudly when the result will be re-aggregated daily, so a half-applied counterfactual cannot be published.
- **verifier:** Verified independently against the actual code. Note: `src/scenario_ops.R` does NOT exist on master or base 7779933 — it is a brand-new theseus file, so the verb mechanism is genuinely refactor-introduced (introduced_by_refactor:true is correct; the finding's master-diff instruction was moot).

The mechanism the finding describes is real and complete:
1. op_set_stacking (scenario_ops.R:361-375) mutates ONLY specs[[auth]]$stacking / program-level $stacking via modifyList — it does not touch the per-authority rate_* columns.
2. The 06 calculator (06_calculate_rates.R:3113-3114) computes total_ra

### [LOW] set_active on ieepa_fentanyl is ignored: calc derives invalidation only from ieepa_reciprocal$active$until
_Counterfactual scenario verbs · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/scenario_ops.R` 329-341
- **problem:** op_set_active accepts any authority and writes $active (authority-level when op$program is NULL, else the program's $active). The doc and the calc make active.until load-bearing ONLY for IEEPA invalidation. But the calculator reads the invalidation date from specs[['ieepa_reciprocal']]$active$until (06_calculate_rates.R:984) and uses that single flag (ieepa_invalidated) to gate BOTH reciprocal AND fentanyl (L1008, L1346). There is no read of ieepa_fentanyl$active$until anywhere. So set_active(ieepa_fentanyl, active=list(until=X)) writes a field the calc never reads — a silent no-op. A user trying to invalidate fentanyl alone, or shift only fentanyl's window, gets no effect. (Related footgun: set_active(ieepa_reciprocal, program='reciprocal', ...) writes program-level active, also ignored — only the no-program/authority-level form reaches L984.)
- **evidence:** 06_calculate_rates.R:984 `ieepa_invalidation <- specs[['ieepa_reciprocal']]$active$until`; L1008 `has_active_ieepa <- !ieepa_invalidated && ...`; L1346 `has_fentanyl <- !ieepa_invalidated && ...`. `grep -n 'ieepa_fentanyl.*active' src/06_calculate_rates.R` returns nothing (no fentanyl active.until read). op_set_active happily mutates specs[['ieepa_fentanyl']]$active with no guard or warning.
- **suggested fix (NOT applied):** Either read ieepa_fentanyl$active$until independently in the calc and gate has_fentanyl on a per-authority invalidation date, or have op_set_active warn/error when targeting ieepa_fentanyl (and when given a program for the IEEPA authorities) so the no-op is not silent.
- **verifier:** I independently verified every factual claim and they hold.

CODE AS CLAIMED:
- src/scenario_ops.R is ENTIRELY NEW on theseus (git confirms "exists on disk, but not in master"; no apply_operations/op_set_active anywhere on master). So introduced_by_refactor=true is literally correct, though there is no master baseline to "drift" from.
- op_set_active (src/scenario_ops.R:331-341) accepts ANY authority and writes $active via modifyList: authority-level when op$program is NULL (L334), else the program's $active (L336-338). No guard, no warning for IEEPA authorities or for the program-level form.


### [LOW] apply_rate_semantics silently coerces a per-row NA floor base to 0 additional duty (NA-base guard only catches the scalar case)
_The generic engine · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/authority_spec.R` 264-275
- **problem:** In floor modes the missing-base guard at line 266 only fires when `base` is a length-1 scalar NA: `if (length(base) == 1L && is.na(base) && any(!is.na(v))) stop(...)`. A VECTORIZED base containing embedded NAs bypasses the guard entirely. Then `out <- pmax(0, v - base)` produces NA for those rows, and the final `out[is.na(out)] <- 0` (line 274) silently converts them to a 0 additional duty rather than erroring. A genuinely-missing post-MFN base for a single product-country row is masked as 'no extra duty' instead of failing loud. This also differs from master: master's inline `pmax(0, ieepa_country_rate - base_rate)` inside a case_when (06_calculate_rates.R master 1059/1125) propagated NA into rate_ieepa_recip; theseus's centralized helper turns that NA into 0. The change is currently latent because the live floor callers coalesce base_rate to 0 first (06_calculate_rates.R:1258 for new_pairs; floor_mask at 2717-2719 requires base_rate < statutory so base is non-NA), so it does not move a number in the frozen golden — which is exactly why the parity gate cannot see it.
- **evidence:** Guard: `if (length(base) == 1L && is.na(base) && any(!is.na(v))) stop('apply_rate_semantics: rate_type=', rate_type, ' requires a numeric base')` then `out <- pmax(0, v - base)` then `out[is.na(out)] <- 0`. For base=c(0.1, NA), v=c(0.5,0.5): guard is FALSE (length(base)!=1), pmax gives c(0.4, NA), NA->0 gives c(0.4, 0). Master had no centralized NA->0 step for the floor case_when arm.
- **suggested fix (NOT applied):** Make the missing-base check vectorized and shape-aware for floor modes, e.g. `if ((length(base)==1L && is.na(base) && any(!is.na(v))) || (length(base)>1L && any(is.na(base) & !is.na(v)))) stop(...)`, so a per-row NA base where the value is in-scope fails loud instead of becoming 0.
- **verifier:** The finding is accurate on every checkable point.

CODE (src/authority_spec.R:261-276, a NEW file on theseus — `git show master:src/authority_spec.R` errors "exists on disk, but not in master"): the quote is exact. Line 266 guard: `if (length(base) == 1L && is.na(base) && any(!is.na(v))) stop(...)`; line 268 `out <- pmax(0, v - base)`; line 274 `out[is.na(out)] <- 0`. The guard is gated on `length(base) == 1L`, so a vectorized base with embedded NAs (e.g. `base=c(0.1, NA)`, `v=c(0.5,0.5)`) bypasses it entirely; `pmax(0, v-base)` yields `c(0.4, NA)`; line 274 then silently coerces the NA row to

### [LOW] The product-keyed half of resolve_rate's precedence (overrides + by_product_tier) is never reached in production — parity gate gives it ZERO coverage
_The generic engine · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/authority_spec.R` 205-233
- **problem:** Every live caller of resolve_rate passes product=NULL: s232_spec_rate (06_calculate_rates.R:294, no product/country), s232_blanket_metal_rate (317, product=NULL), s122_value (2556, no product/country), and scenario_ops.R:104 (no product/country). With product=NULL the overrides branch can never match (entry-form: `hit_p <- !is.null(product) && ...` is always FALSE; named-scalar form: `if (!is.null(product) && ...)` always skipped) and the by_product_tier branch can never match (`!is.null(product)` guard at line 230 is FALSE). The live 301 tier is instead read by a DIRECT field access + hts8 join (06_calculate_rates.R:2399 `specs[['section_301']]$programs[[1]]$rate$by_product_tier`), and 232 deals via s232_deal_records, bypassing resolve_rate. So the entire most-specific half of the keystone resolver's precedence ladder is exercised ONLY by tests/test_resolve_rate.R, never by the pipeline. Any logic/precedence bug introduced there would be invisible to BOTH the numeric parity gate AND production output — precisely the blind spot the audit targets. (Not an active bug: the implemented logic is correct per the unit test; flagged because the doc presents resolve_rate as 'the keystone every authority depends on' when half its branches are load-bearing only in tests.)
- **evidence:** All four production call sites: `resolve_rate(progs[[pos]]$rate)$value` (294), `resolve_rate(prog_rate, product = NULL, country = cty)$value` (317), `resolve_rate(specs[['section_122']]$programs[[1]]$rate)$value` (2556), `resolve_rate(progs[[i]]$rate)$value` (scenario_ops 104). None passes a non-NULL product. The 301 by_product_tier is read directly at 06_calculate_rates.R:2399, not via resolve_rate.
- **suggested fix (NOT applied):** Either route the live 301/deal reads through resolve_rate(product=...) so the precedence is actually exercised end-to-end, or document explicitly that overrides/by_product_tier in resolve_rate are test-only scaffolding for future planks and keep the unit test as the sole guarantor (and treat it as a required gate, since parity cannot cover it).
- **verifier:** Independently verified all claims against the real code (git show theseus + Read of current src/authority_spec.R).

CORE CLAIM CONFIRMED. There are exactly 4 production resolve_rate() invocations and every one passes product=NULL:
- 06_calculate_rates.R:294 `resolve_rate(progs[[pos]]$rate)$value` (s232_spec_rate, no product arg => NULL)
- 06_calculate_rates.R:317 `resolve_rate(prog_rate, product = NULL, country = cty)$value` (s232_blanket_metal_rate, explicit NULL)
- 06_calculate_rates.R:2480 `resolve_rate(specs[['section_122']]$programs[[1]]$rate)$value` (s122_value, no product)
- scenario_op

### [LOW] Program-level set_stacking is a silent no-op: the policy builder only reads authority-level stacking$class
_Stacking Generalization · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/stacking.R, src/scenario_ops.R` stacking.R:213; scenario_ops.R:369-373
- **problem:** op_set_stacking accepts a program-targeted op (op$program set) and mutates specs[[auth]]$programs[[pos]]$stacking, but stacking_policy_from_specs reads ONLY the authority-level field specs[[auth]]$stacking$class (stacking.R:213). No code path reads program-level stacking into the live policy. So a program-level set_stacking validates, mutates the spec, and has ZERO effect on the calculator — a silent no-op. The op_set_stacking docstring claims set_stacking is 'load-bearing in the CALCULATOR' and 'Applies to ANY authority', which is true only for authority-level ops.
- **evidence:** stacking.R:213 `cls <- map_class(spec$stacking$class %||% NA_character_)` reads the authority-level stacking only. scenario_ops.R:370-372 writes `specs[[auth]]$programs[[pos]]$stacking <- modifyList(...)`. grep confirms stacking.R:213 is the ONLY reader of a spec stacking class into the policy; nothing reads programs[[i]]$stacking. The adapter even sets per-program classes (authority_adapter.R:402 'primary_metal', :405 'primary_full') that the policy builder never consumes — they are dead metadata.
- **suggested fix (NOT applied):** Either (a) reject program-targeted set_stacking ops with a fail-loud error (the engine is authority-grained — one rate_232 column — so per-program stacking cannot be expressed), or (b) document explicitly that program-level stacking is metadata-only and not load-bearing, and remove the 'Applies to ANY authority' / program branch from op_set_stacking to prevent a false sense of effect.
- **verifier:** I independently verified every factual claim in the finding by reading the actual code.

1. READER IS AUTHORITY-LEVEL ONLY. In src/stacking.R, stacking_policy_from_specs() loops over a fixed skeleton; line 213 sets `spec <- specs[[e$auth]]` (the AUTHORITY spec) and line 219 reads `cls <- map_class(spec$stacking$class %||% NA_character_)` plus line 221 `spec$stacking$exceptions`. (The finding cites line 213 as the reader; the actual class-read is line 219 — `spec` is bound at 213. Minor line-number imprecision, mechanism exact.) Nothing in the loop touches `spec$programs[[...]]$stacking`.

2. W

### [LOW] IEEPA universal-exempt list gained date-windowing vs the master baseline — real behavior change, invisible to the parity gate (baked into the re-frozen golden)
_timing / date-gating / cross-revision leakage · pre-existing · verdict=confirmed · finder-confidence=high_
- **file:** `src/authority_adapter.R` 314-335 (adapter helper); consumed at src/06_calculate_rates.R:1043-1047,1215,1286
- **problem:** The adapter helper .resolve_ieepa_exempt_products() filters the universal Annex II exempt list by effective_date_start/_end against the revision date (lines 324-333). Its docstring (lines 312-314) says 'VERBATIM from 06_calculate_rates.R (the old inline load)' and the Pass-1.5 commit (55090cc) says 'Bit-exact by construction'. But the OLD inline load at the reviewer's master baseline (merge-base 7779933) was date-BLIND: it read only the hts10 column with no date filter, and the CSV at that baseline had no date columns. So relative to master, theseus now exempts FEWER products from the IEEPA reciprocal surcharge in early/mid revisions: 594 of 5053 exempt rows carry start dates 2025-04-05 / 2025-09-08 / 2025-11-13, and the universal list masks rate_ieepa_recip (06:1215 is_universally_exempt). For rev_7..rev_28 (before 2025-09-08 / 2025-11-13) the 2025-09-08 and 2025-11-13 codes are NOT exempt in theseus but WERE exempt (retroactively) in master, raising reciprocal coverage. Critically, the parity gate cannot catch this: the change landed at eb145ba which is an ancestor of BOTH golden refreezes (70b6b97 frozen at e304912, then 6ec81b9/52dab78), so candidate==golden by construction — exactly the central blind spot. (Fentanyl is unaffected: the universal list does not mask fentanyl — only its Ch98 subset does, 06:1417-1425, and all Ch98 codes are date-NA.)
- **evidence:** git show 7779933:src/06_calculate_rates.R line 791: `read_csv(ieepa_exempt_path, col_types = cols(hts10 = col_character()))$hts10` — no date filter. git show 7779933:resources/ieepa_exempt_products.csv header = `hts10` only. Current CSV header = `hts10,effective_date_start,effective_date_end` with 594 dated rows (start dates 2025-04-05/09-08/11-13). Adapter lines 324-333 add `filter(is.na(effective_date_start) | as.Date(effective_date_start) <= rd_exempt)` and the _end mirror. `git merge-base --is-ancestor eb145ba e304912` => YES (baked into 70b6b97 golden).
- **suggested fix (NOT applied):** This is the intended policy behavior (a ported origin/master fix, c5b2eb1, that prevents retroactive Annex II amendments — without it the static list over-exempts ~$ of early-2025 reciprocal duty). No code change needed, but (a) correct the misleading 'VERBATIM from the old inline load' / 'bit-exact' wording in authority_adapter.R:312-314 and the Pass-1.5 commit narrative — it is verbatim only relative to the immediate theseus predecessor (which already had the gate via eb145ba), not relative to master; and (b) because the parity gate is structurally blind to it, add an explicit oracle/regression assertion that an early revision (e.g. rev_7) does NOT exempt the 2025-11-13-dated codes from reciprocal, so the date-window cannot silently regress.
- **verifier:** All factual claims independently verified against the real code, not the finding's quoted snippets.

(1) CODE DOES WHAT'S CLAIMED. Current src/authority_adapter.R:314-335 — .resolve_ieepa_exempt_products() reads ieepa_exempt_products.csv and applies `filter(is.na(effective_date_start) | as.Date(effective_date_start) <= rd_exempt)` (lines 324-328) plus the _end mirror (329-333). Its docstring (312-313) literally says "VERBATIM from 06_calculate_rates.R (the old inline load)" and the comment block (304) says "bit-exact (oracle-tested)".

(2) MASTER BASELINE WAS DATE-BLIND. merge-base(theseus, ma

### [LOW] Published weighted-ETR aggregation has ZERO parity coverage under the standard --unweighted gate, while production publishes weighted numbers
_PUBLISH / DAILY-AGGREGATION / PARITY-TOOLING INTEGRITY · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/parity.R` 94-105, 144-146
- **problem:** .parity_is_ungated_weighted_col() skips every golden-only column matching `^weighted_etr|^etr_|_imports_b$`. The standard gate is invoked with GATHER_ARGS="--unweighted" (submit_build_array.sh:42, plan doc:246/576) so the candidate omits all weighted columns, while the frozen golden tests/golden/52dab78 was built WEIGHTED (its daily_overall.csv carries weighted_etr, weighted_etr_additional, matched_imports_b, total_imports_b and daily_by_authority.csv carries etr_232..etr_base). Result: when the gate runs unweighted, the entire import-weighted aggregation (the inner_join to imports, the sum(total_rate*imports)/total_imports math, country/sector denominators, the etr_base residual) is silently skipped and NEVER value-compared — yet tariff-model consumes the weighted published numbers. The streaming/parts-cache relocation of this exact weighting math is therefore unvalidated by the gate in its normal mode.
- **evidence:** src/parity.R:103-105 `.parity_is_ungated_weighted_col <- function(col) grepl('^weighted_etr|^etr_|_imports_b$', col)`; lines 144-146 strip those from only_golden so no schema_missing_column fires. scripts/submit_build_array.sh:42-43 propagates --unweighted to the build. Golden header (tests/golden/52dab78/daily/daily_overall.csv) includes weighted_etr,weighted_etr_additional,matched_imports_b,total_imports_b — present in golden, absent in an --unweighted candidate, hence skipped.
- **suggested fix (NOT applied):** Run at least one parity gate WEIGHTED (candidate built with import weights) against the weighted golden so the weighted-ETR aggregation is value-compared, or freeze a separate unweighted golden so unweighted candidates compare against an unweighted reference with no skip. The snapshot artifact gates the source rates, but the weighting/denominator math is currently unverified end-to-end.
- **verifier:** The test-gap mechanism is REAL and the cited code does exactly what the finding claims. Verified against the actual code (src/parity.R is a NEW file on theseus; `git show master:src/parity.R` fatals — does not exist on master, so this is refactor-introduced):

(1) src/parity.R:103-105 — `.parity_is_ungated_weighted_col <- function(col) grepl('^weighted_etr|^etr_|_imports_b$', col)` matches the quote exactly.
(2) src/parity.R:144-146 — `skipped_ungated <- only_golden[.parity_is_ungated_weighted_col(only_golden)]; only_golden <- setdiff(only_golden, skipped_ungated)` — strips those from only_gol

### [MEDIUM] No committed test asserts STREAMING == MONOLITH for daily aggregates / quality report; equivalence rests on a one-off Slurm job + the re-frozen golden
_PUBLISH / DAILY-AGGREGATION / PARITY-TOOLING INTEGRITY · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/09_daily_series.R` 1001-1067, 924-975
- **problem:** The doc claims 'quality equivalence 13800647 proved STREAMING == MONOLITH' (plan doc:801) and that the streaming daily output is 'identical' to build_daily_aggregates(combined_ts). But there is no committed unit/integration test that builds a small multi-revision combined timeseries, runs build_daily_aggregates(combined) AND the parts-cache / build_daily_aggregates_streaming path, and asserts equality (grep over tests/ for build_daily_aggregates_streaming, build_quality_inputs_streaming, load_daily_parts returns nothing). run_tests_daily_series.R only exercises build_daily_aggregates() in-memory. The sole equivalence evidence is a transient Slurm job and the numeric parity gate vs a golden that was re-frozen AFTER the streaming work landed (tests/golden/70b6b97 then 52dab78). Per the central blind spot, 'GREEN vs golden' cannot catch a streaming relocation that moved a number and was baked into the re-frozen golden, nor a bug present identically in both paths. The schema-NA union accounting (quality_report.R:424-432), the per-revision n_all_pairs denominators, and the parts-bind ordering are all plausible divergence points that no standing test pins.
- **evidence:** No tests/ file references build_daily_aggregates_streaming / build_quality_inputs_streaming / load_daily_parts_if_complete (grep returned nothing). docs/spec_driven_calculator_plan.md:801 'quality equivalence 13800647 proved STREAMING == MONOLITH' — a job id, not a repo test. The golden was re-frozen twice after this work (commits f09d279 -> 52dab78).
- **suggested fix (NOT applied):** Add a fast unit test on a synthetic 3-revision fixture that (a) assembles a combined ts and runs build_daily_aggregates(combined), (b) writes per-revision snapshots + daily parts and runs the parts-cache/streaming path, and (c) asserts compare_parity() is clean for daily_overall/by_authority/by_country/by_category AND that build_quality_inputs_streaming() reproduces check_schema()/compute_revision_quality() on the bound monolith. This converts the doc claim into a regression guard the golden re-freeze cannot mask.
- **verifier:** I independently verified every load-bearing claim in the finding.

1) The streaming functions exist exactly as cited. src/09_daily_series.R defines build_daily_aggregates_streaming() (line 1001-1067), load_daily_parts_if_complete() (924-975), and src/quality_report.R defines build_quality_inputs_streaming() (line 378). The cited line ranges match.

2) introduced_by_refactor:true is correct. git show master:src/09_daily_series.R has NO streaming functions — master's run_daily_series(ts,...) only takes the combined ts and there is no build_daily_aggregates_streaming/load_daily_parts_if_complete;

### [MEDIUM] Absolute-invariant suites (the guard against golden-blindness) silently exit 0 when build data is absent
_TEST QUALITY / PARITY-BLINDNESS / COVERAGE GAPS · pre-existing · verdict=confirmed · finder-confidence=high_
- **file:** `tests/test_timeline_invariants.R` 34-49
- **problem:** The docs correctly state that parity vs the (twice-re-frozen) golden is blind to latent golden bugs and that absolute-invariant tests are the only guard (spec_driven_calculator_plan.md:596-600). But those guards are themselves data-gated to a no-op-green. test_timeline_invariants.R's skip_all() (lines 34-38) calls quit(save='no', status=0) when data/timeseries lacks snapshot_*.rds or bnd_ files (lines 46-49), so a CI gate keying on exit code sees GREEN having asserted NOTHING. The same pattern holds for test_rate_calculation.R (run_test treats skip as neither pass nor fail; the script only quit(status=1) if fail_count>0 — so a run where all the Russia/semi/subdivision-r absolute tests skip for want of snapshots exits 0 despite the latent leak). Because the snapshot data is gitignored build output, a fresh checkout / standard CI runs these 'invariant guards' as silent green. They only actually guard when someone has the full build tree locally (as here). So the safety net the doc relies on to backstop the parity blind spot is conditional on data that CI does not have.
- **evidence:** test_timeline_invariants.R:37 `quit(save = 'no', status = 0)` inside skip_all; lines 46-49 trigger it when snap_files / bnd_files are empty. test_rate_calculation.R:33-45 run_test counts skip separately; tail (line ~960) `if (fail_count > 0) quit(status = 1)` — skips never set fail_count. 48 skip_test guards in test_rate_calculation.R, all gated on snapshot_*.rds / resource files. (Confirmed locally the data IS present so the tests run; the gap is the silent-green behavior absent that data.)
- **suggested fix (NOT applied):** Make the absolute suites fail (or at minimum exit non-zero / emit a machine-detectable SKIPPED marker the CI treats as not-pass) when their required artifacts are missing in the environment that is supposed to have them, OR run them only in a build-data-present job and assert in CI config that that job actually executed the assertions (e.g. assert pass_count above a floor). A skipped invariant must not be indistinguishable from a passing one in the exit code.
- **verifier:** Verified at the code level (not via the quoted snippet). test_timeline_invariants.R is new on theseus (absent in master). Lines 34-38 define skip_all() which calls quit(save='no', status=0); lines 46-49 invoke it when snap_files OR bnd_files are empty — exactly as the finding states. In test_rate_calculation.R, run_test (lines 33-45) routes skip conditions to skip_count only (38-40) and errors to fail_count (41-43); the sole nonzero-exit path is the trailing `if (fail_count > 0) quit(status = 1)`, so a fully-skipped run exits 0. There are 48 skip_test guards, all gated on snapshot_*.rds / reso

### [LOW] daily_part cache staleness check ignores policy_params content (expiry dates / CTY_CHINA), only mtime+interval+weight_mode
_PUBLISH / DAILY-AGGREGATION / PARITY-TOOLING INTEGRITY · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/09_daily_series.R` 943, 954-958
- **problem:** load_daily_parts_if_complete() validates a cached daily_part_<rev>.rds against {revision, valid_from, valid_until, weight_mode} plus an mtime check (part newer than its snapshot). It does NOT validate the policy_params used to compute the part. The part is produced by write_daily_part_for_snapshot()->build_daily_aggregates(), whose output depends on policy_params via expiry_boundaries()/apply_expiry_zeroing() (s122/Swiss expiry split + zeroing) and CTY_CHINA. If a build reuses on-disk snapshots (incremental / --start-from) while config/policy_params.yaml changed (e.g. an s122 expiry date moved, or finalized flipped), the snapshot mtime is unchanged, the interval+weight_mode still match, and the stale part is ACCEPTED — publishing daily numbers computed against the OLD policy. Narrow exposure (within a single fresh array build pp is consistent), but a real correctness gap for incremental rebuilds.
- **evidence:** src/09_daily_series.R:943 `if (!file.exists(snapshot_path) || file.info(path)$mtime < file.info(snapshot_path)$mtime)` and 954-958 `valid <- identical(...meta$revision...) && identical(...valid_from...) && identical(...valid_until...) && identical(...weight_mode...)` — no policy_params hash/version. The part metadata block (884-911) records no policy fingerprint.
- **suggested fix (NOT applied):** Record a fingerprint of the policy-relevant inputs (e.g. digest of collect_expiry_adjustments(pp) + CTY_CHINA + stacking_method + SERIES_HORIZON_END) in the part metadata, and require it to match in load_daily_parts_if_complete(); or invalidate the cache when config/policy_params.yaml mtime is newer than the part.
- **verifier:** Verified independently against the real code (not the finding's snippet).

CODE MATCHES THE CLAIM. src/09_daily_series.R:943 is exactly `if (!file.exists(snapshot_path) || file.info(path)$mtime < file.info(snapshot_path)$mtime)` and 954-958 validate only `identical(meta$revision...) && identical(valid_from) && identical(valid_until) && identical(weight_mode)`. The part metadata block (902-911) records revision/valid_from/valid_until/weight_mode/n_snapshot_rows/created_at — NO policy fingerprint. A repo-wide grep for digest/fingerprint/policy-mtime returns NONE.

THE DEPENDENCY IS REAL. The cac

### [LOW] Parity comparator keys wide daily artifacts on `date` alone — a duplicate-date bug would explode the join rather than report cleanly, and is unverified by any keyed assertion
_PUBLISH / DAILY-AGGREGATION / PARITY-TOOLING INTEGRITY · refactor · verdict=confirmed · finder-confidence=medium_
- **file:** `src/parity.R` 262-263, 153
- **problem:** PARITY_ARTIFACTS keys daily_overall and daily_by_authority on c('date') only. compare_parity() does a dplyr::full_join by date (line 153). Today daily_overall/by_authority rows are unique per date (intervals are gapless+exclusive and sub-interval splits stay within a revision), so the 1:1 join is correct. But if a future interval/split/mint bug produced a duplicate `date` on either side, the full_join becomes many-to-many: it would either silently cartesian-expand the comparison or emit a relationship warning, instead of surfacing the duplicate as a first-class violation. The gate has no explicit uniqueness assertion on the natural key, so a duplicate-date defect is not cleanly caught — and it would be masked exactly when interval logic is the thing under test.
- **evidence:** src/parity.R:262 `daily_overall = list(glob='daily_overall*.csv', key_cols=c('date'))`; line 153 `joined <- dplyr::full_join(a, g, by=key_cols, suffix=...)` with no prior distinct()/anyDuplicated() guard on key_cols.
- **suggested fix (NOT applied):** Add an explicit uniqueness check in compare_parity(): if anyDuplicated(actual[key_cols]) or anyDuplicated(golden[key_cols]) > 0, record a 'duplicate_key' violation (fail) before the join, so a duplicated date/interval is reported rather than silently cartesian-joined.
- **verifier:** All claims verified against the real code and empirically reproduced.

CITED CODE MATCHES: src/parity.R:262-263 keys daily_overall and daily_by_authority on c('date') only; line 153 is `joined <- dplyr::full_join(a, g, by = key_cols, suffix = c('.actual','.golden'))` with no distinct()/anyDuplicated()/relationship= guard before it. Confirmed by Read, not the finding's snippet.

INTRODUCED BY REFACTOR: confirmed TRUE — `git show master:src/parity.R` errors ("exists on disk, but not in master"); the 7779933...theseus diff shows the file as brand new (@@ -0,0 +1,296 @@). The whole comparator is r

### [LOW] §232 deal relocation globally reorders surcharge-before-floor vs master's tibble row order (latent parity-drift, golden-blind)
_dead-code / vestigial / completeness / missing-things · refactor · verdict=confirmed · finder-confidence=low_
- **file:** `src/06_calculate_rates.R` 331-345 (s232_deal_records); 1863-1918 (auto deal loop)
- **problem:** On master, auto_deal_rates/wood_deal_rates were a single tibble iterated row-by-row, so surcharge and floor deal rows were applied in tibble order via successive last-write-wins if_else mutations on rate_232. On theseus the adapter (.s232_deal_layers, authority_adapter.R:143) splits the tibble into rate$overrides (surcharge) and rate$floors, and s232_deal_records() returns ALL overrides first, then ALL floors. So the global application order is now always surcharge-then-floor regardless of the original interleaving. For the current config (UK surcharge on autos; EU/JP/KR floors — disjoint country sets) the final rate_232 is identical, so baseline parity holds and this got baked into the re-frozen golden. But it is a behavior change the parity gate cannot detect: a future deal config (or a scenario) that puts the same country+product under BOTH a surcharge and a floor would resolve differently (theseus: floor always wins; master: tibble-order wins).
- **evidence:** 06_calculate_rates.R:336-343 builds recs as overrides (rate_type='surcharge') THEN floors (rate_type='floor'); the deal loop at 1896-1914 does last-write-wins if_else on rate_232. Master applied rows in auto_deal_rates order (git show master:src/06_calculate_rates.R lines 1717-1768). No current data exercises an overlap, so golden re-freeze is blind to it.
- **suggested fix (NOT applied):** If deal records can ever overlap on a country+product, preserve master's original tibble row order when emitting records (interleave overrides and floors per source row) rather than overrides-then-floors; or assert disjointness of surcharge vs floor scopes in .s232_deal_layers so an overlapping config fails loud instead of silently letting floor win.
- **verifier:** Independently verified against master and theseus; every load-bearing claim holds.

MECHANISM (real): On master, auto_deal_rates/wood_deal_rates are single tibbles built in src/05_parse_policy_params.R (auto: lines ~925-928; wood: ~970-973) whose row order follows the ch99 source stream, interleaving floor and surcharge rows. The master deal loop (git show master:src/06_calculate_rates.R:1714-1767 autos, 1791-1818 wood) iterates those rows in source order doing last-write-wins if_else mutations on rate_232, so for any double-hit cell the LAST row in ch99 order wins (data-order dependent — coul

### [LOW] Doc/memory claims contradicted: run_comparisons.R NOT deleted, iso_to_census_vec NOT fully removed
_dead-code / vestigial / completeness / missing-things · pre-existing · verdict=confirmed · finder-confidence=high_
- **file:** `src/run_comparisons.R` 1-216 (file present); src/06_calculate_rates.R:2198 (closure present)
- **problem:** The audit charter (echoing the docs/memory) lists run_comparisons.R among files 'deleted on theseus' and states the iso_to_census_vec closures 'the doc says were deleted — confirm they're gone'. Neither is true. src/run_comparisons.R still exists unchanged on theseus and is functional (degrades gracefully when data/tpc is absent). iso_to_census_vec is removed ONLY from the §232 auto/wood deals blob (master:06 line 1703, now replaced by spec-driven s232_deal_records); it SURVIVES as a live local closure in the subdivision-(r) FTA-share block at 06:2198 (used at 06:2206), bit-identical to master:06:2154, and also exists in generate_etrs_config.R:432. This is a documentation accuracy problem, not a runtime bug, but it means the de-blob 'closures deleted' claim is only partially true and could mislead a future reader into thinking the subdiv-r census expansion was relocated when it was not.
- **evidence:** git ls shows src/run_comparisons.R present (39-line diff = a streaming-refactor touch, not a deletion). grep iso_to_census_vec -> src/06_calculate_rates.R:2198 (def) + :2206 (call), src/generate_etrs_config.R:432/444. master:06:1703 (deals closure, gone on theseus) vs master:06:2154 (subdiv-r closure, retained verbatim on theseus).
- **suggested fix (NOT applied):** Correct the docs/memory: run_comparisons.R was NOT deleted (only scenarios.yaml/08_weighted_etr.R/apply_scenarios.R/test_tpc_comparison.R/data/tpc were); and only the §232 deals iso_to_census_vec closure was removed — the subdivision-(r) and generate_etrs_config closures remain calc-side by design.
- **verifier:** Independently verified every factual claim against the real code on the theseus branch (HEAD ff8725a); all check out.

(1) run_comparisons.R NOT deleted: `git cat-file -e` confirms src/run_comparisons.R exists on BOTH master and theseus. `git diff 7779933...theseus` shows only a 39-line deletion (the run_etr_comparison weighted-ETR overlay function + its --etr dispatch), a streaming-refactor touch, not a file removal. It degrades gracefully when data/tpc is absent (file.exists/dir.exists guards at lines 55, 64, 182-189; prints "TPC benchmark not available — skipping" rather than erroring). The

### [LOW] Auxiliary ETR export/compare tools re-stack with the default policy (same specs-less pattern), so an exported counterfactual snapshot mis-decomposes
_Stacking generalization · pre-existing · verdict=confirmed · finder-confidence=medium_
- **file:** `src/export_for_etrs.R` 152 (and src/compare_etrs.R:134)
- **problem:** export_for_etrs.R:152 calls apply_stacking_rules(snapshot) and compare_etrs.R:134 calls apply_stacking_rules(rates, cty_china = cty_china), both WITHOUT a stacking_policy, so both reconstruct total_rate/total_additional using default_stacking_policy(). For baseline this is correct (default == spec), but these tools would mis-stack a snapshot produced under a set_stacking counterfactual — the same latent gap as the 09 daily re-stack, in the externally-facing comparison/export path. These are ad-hoc tools not wired into 00_build, so production output is unaffected; carried over from master (apply_stacking_rules predates the policy argument).
- **evidence:** export_for_etrs.R:152 `snapshot <- apply_stacking_rules(snapshot)`; compare_etrs.R:134 `rates <- apply_stacking_rules(rates, cty_china = cty_china)` — neither passes stacking_policy=, so stacking.R:290 `policy <- stacking_policy %||% default_stacking_policy(cty_china)` selects the default. Neither file is referenced by src/00_build_timeseries.R / src/main.R / run_*.R (grep returned no matches).
- **suggested fix (NOT applied):** If these tools are ever run against a counterfactual snapshot, accept and forward a stacking policy (or read a persisted policy/net_* off the snapshot) instead of defaulting. For baseline runs no change is needed. Lowest-risk: document that these tools assume the baseline stacking policy and are not valid for set_stacking counterfactuals.
- **verifier:** Verified every cited line independently. export_for_etrs.R:152 is exactly `snapshot <- apply_stacking_rules(snapshot)` and compare_etrs.R:134 is exactly `rates <- apply_stacking_rules(rates, cty_china = cty_china)` — neither passes `stacking_policy=`. stacking.R:290 confirms `policy <- stacking_policy %||% default_stacking_policy(cty_china)`, so absent a policy both calls reconstruct total_rate/total_additional via the DEFAULT stacking policy. The set_stacking counterfactual verb genuinely exists (scenario_ops.R:361) and the codebase ALREADY documents this exact latent gap (scenario_ops.R:355-

### [LOW] is_country_eo_exempt gained two exemption terms (India Annex-II inheritance + Ch98 carve-out) — moves country-EO surcharge vs master, hidden by re-frozen golden
_IEEPA de-blob parity · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/06_calculate_rates.R` 1150-1154, 1221-1225
- **problem:** theseus expanded is_country_eo_exempt from master's single-term test (`paste(country_eo_ch99, substr(hts10,1,8)) %in% country_eo_exempt_keys`) to a three-term OR that additionally exempts (a) products on the universal Annex-II list when the country-EO is in country_eo_annex_ii_inherit (India 9903.01.84, per note 2(z)(ii)), and (b) any ch98 good (hts10 %in% ch98_eo_exempt). This zeroes the country-EO surcharge (the +40% Brazil / +25% India component) for those products where master would have kept it. It is the correct policy, but it changes rate_ieepa_recip for India + ch98 goods relative to master and was baked into the re-frozen golden, so the parity gate cannot see it.
- **evidence:** theseus 06:1150-1154 adds `| (country_eo_ch99 %in% annex_ii_inherit_eos & is_universally_exempt) | hts10 %in% ch98_eo_exempt`; master:src/06_calculate_rates.R ~1044-1045 has only the single country_eo_exempt_keys term. `git log -S annex_ii_inherit_eos 7779933..theseus` → 70b6b97 'Port master fix2+fix7'. config/policy_params.yaml:708 `country_eo_annex_ii_inherit: ['9903.01.84']` supports the India term.
- **suggested fix (NOT applied):** No code change — this is an intentional ported fix and matches India note 2(z)(ii) + the standard ch98 paragraph carried by every country-EO note. Flag only: the 'master fix port' commits reference master SHAs (c471770/b10ce94, 38757f6/c5b2eb1) that do not exist in this repo's `master`, so the audit baseline and the documented baseline diverge; note in the plan doc that golden 70b6b97 onward is NOT byte-comparable to the `master` rate output for country-EO/ch98 product cells.
- **verifier:** Verified independently against the real code. master HEAD in this repo IS 7779933 (the audit baseline). git show master:src/06_calculate_rates.R has the single-term is_country_eo_exempt at lines 1044-1045 and 1112-1113: `!is.na(country_eo_ch99) & paste(country_eo_ch99, substr(hts10,1,8), sep='|') %in% country_eo_exempt_keys`. Current src/06_calculate_rates.R at lines 1216-1220 (and the duplicate new_pairs block at 1287-1291) expands this to a three-term OR adding (a) `(country_eo_ch99 %in% annex_ii_inherit_eos & is_universally_exempt)` and (b) `hts10 %in% ch98_eo_exempt`. annex_ii_inherit_eos 

### [LOW] UK/Japan civil-aircraft 232 exemptions (9903.96.01/.02) mis-bucketed in scrape_us_notes FLOOR_EXEMPTIONS (pre-existing, self-flagged)
_Master-fix port + revision re-dating · pre-existing · verdict=confirmed · finder-confidence=medium_
- **file:** `src/scrape_us_notes.R` 108-114 (the theseus-added NOTE comment) referencing the FLOOR_EXEMPTIONS tribble below it
- **problem:** A theseus-added comment documents that 9903.96.01/.02 (UK/Japan civil-aircraft) entries 'remain mis-bucketed here as a pre-existing, low-impact issue' — they are section-232 metals-annex carve-outs (analogous to Taiwan's 9903.96.03, which IS routed correctly via the section_232_aircraft_exemption path) but are listed in scrape_us_notes.R's FLOOR_EXEMPTIONS table, i.e. treated as floor/reciprocal exemptions rather than 232-annex exemptions. The mis-bucketing predates theseus; the refactor only added the documenting comment (the only change to this file, and it is comment-only).
- **evidence:** git diff 7779933...theseus -- src/scrape_us_notes.R shows the sole change is a +7-line comment: 'The 9903.96.01/.02 UK/Japan entries below are the analogous 232 carve-outs but remain mis-bucketed here as a pre-existing, low-impact issue — see todo.md.' This is a resource-regen scraper, so impact is limited to the regenerated floor-exemption resource, but a mis-bucketed UK/Japan aircraft line would get floor-exemption treatment instead of (or in addition to) the correct 232-annex carve-out.
- **suggested fix (NOT applied):** Route 9903.96.01/.02 through the same section_232_aircraft_exemption + per-country aircraft-exempt CSV path that 9903.96.03 (Taiwan) uses, and remove them from FLOOR_EXEMPTIONS, then regenerate the affected resource. Quantify before/after on UK/Japan aircraft HS lines to confirm 'low-impact'.
- **verifier:** Independently verified against the real code. The diff `git diff 7779933...theseus -- src/scrape_us_notes.R` is exactly 7 added lines, ALL comment lines (each starts with '#'), zero removed lines, zero code changes — confirming the refactor's sole contribution is a documenting comment and `introduced_by_refactor: false` is correct. The comment self-flags the condition verbatim: "The 9903.96.01/.02 UK/Japan entries below are the analogous 232 carve-outs but remain mis-bucketed here as a pre-existing, low-impact issue."

The substantive condition is real: `9903.96.02` (Japan civil_aircraft) IS i

### [LOW] Standalone daily-series path defaults use_policy_dates=TRUE when rev_dates not threaded — latent date-mode mismatch under --use-hts-dates
_Master-fix port + revision re-dating · pre-existing · verdict=confirmed · finder-confidence=medium_
- **file:** `src/09_daily_series.R` 1083 (run_daily_series snapshot_dir branch) and 1327 (09 __main__)
- **problem:** run_daily_series()'s snapshot_dir!=NULL branch does 'if (is.null(rev_dates)) rev_dates <- load_revision_dates()' with no use_policy_dates argument, so it falls back to the default TRUE. Likewise 09's own __main__ at line 1327 calls load_revision_dates(revision_dates_path) with the default. If a build is produced with --use-hts-dates (snapshots stamped with raw HTS effective dates) and the daily series is then (re)built via a caller that does NOT thread rev_dates, the interval boundaries would be computed from POLICY dates while the snapshots carry HTS dates, mis-assigning which revision is live on which day. The primary build path (00_build_timeseries.R:1141/1161) is SAFE because it passes the in-memory `ts` (snapshot_dir=NULL -> build_daily_aggregates uses dates baked into ts), and scripts/build_gather.R IS safe because it threads use_policy_dates (line 54). So today this is latent fragility, not an active bug, but the re-dating made the policy-vs-HTS distinction load-bearing and there is no guard tying snapshot date-mode to daily date-mode.
- **evidence:** src/09_daily_series.R:1083 'if (is.null(rev_dates)) rev_dates <- load_revision_dates()'; line 1327 'rev_dates <- load_revision_dates(revision_dates_path)'. revisions.R:78-79 default use_policy_dates=TRUE. scripts/build_gather.R:39,54 correctly derives '!('--use-hts-dates' %in% args)' and threads it.
- **suggested fix (NOT applied):** Persist the use_policy_dates flag into the build manifest and have run_daily_series / 09 __main__ read it (or require rev_dates to be passed), so the daily series cannot silently use a different date mode than the snapshots it aggregates. At minimum add an assertion that snapshot min/max effective_date is consistent with the loaded rev_dates date range.
- **verifier:** Independently verified every load-bearing claim in the actual code (not the finding's snippet).

(1) src/09_daily_series.R:1083 reads `if (is.null(rev_dates)) rev_dates <- load_revision_dates()` inside run_daily_series()'s snapshot_dir!=NULL branch — no use_policy_dates arg, so it inherits the default. (2) src/revisions.R:78-79 confirms `load_revision_dates(csv_path=..., use_policy_dates = TRUE)` — default TRUE. (3) Line 1327 (`rev_dates <- load_revision_dates(revision_dates_path)`) is inside build_alternative_timeseries, also defaulting; this exact call is present in master:1124 too, consiste

### [LOW] s122 missing-file branch silently drops master's WARNING message
_PASS-1.5 EXEMPT-SETS-IN-SPEC · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/authority_adapter.R` 361-365
- **problem:** .resolve_s122_exempt returns character(0) silently when s122_exempt_products.csv is absent. The original calc emitted `message('  WARNING: s122_exempt_products.csv not found — no exemptions applied')`. The numeric result is identical (no products exempt -> s122 applied to everything), but the operator-facing warning that a curated exemption file went missing is lost. Purely a logging/observability regression, not a rate change.
- **evidence:** origin/master src/06_calculate_rates.R: `s122_exempt_hts8 <- if (file.exists(s122_exempt_path)) { read_csv(...)$hts8 } else { message('  WARNING: s122_exempt_products.csv not found — no exemptions applied'); character(0) }`. Adapter: `if (!file.exists(s122_exempt_path)) return(character(0))` — no message.
- **suggested fix (NOT applied):** Add the same warning message in the !file.exists branch of .resolve_s122_exempt (and similarly the adapter could preserve the ieepa warning, which it does keep). Optional, since the file is present in the repo.
- **verifier:** Verified directly against the code. Current src/authority_adapter.R lines 361-365 (.resolve_s122_exempt) returns character(0) silently when resources/s122_exempt_products.csv is absent, with no message. Master's src/06_calculate_rates.R (lines 2350-2355) emitted message('  WARNING: s122_exempt_products.csv not found — no exemptions applied') in the else branch. The git diff 7779933...theseus confirms .resolve_s122_exempt is a newly added function (+ lines 367-370) and the inline warning is gone — so this is refactor-introduced.

Numeric parity holds: tracing both paths, an absent file yields a

### [LOW] set_rate IEEPA with rate=0 leaves a live 0 by_country entry, keeping has_active_ieepa/has_fentanyl ON instead of turning the country off
_Counterfactual scenario verbs · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/scenario_ops.R` scenario_ops.R:402-424 (IEEPA set_rate); gate read 06_calculate_rates.R:942,1280-1281
- **problem:** op_set_rate for IEEPA does bc[[c]] <- rate even when rate==0, leaving a 0-valued by_country entry. The calc gates on length(by_country) > 0 (06:942 reciprocal, 06:1280-1281 fentanyl), so a set_rate(country, 0) keeps the gate ON. For a country that was previously OFF (gate was off because by_country empty), set_rate(X, 0) flips has_active_ieepa to TRUE, which then causes the universal baseline (default_unlisted_rate, if set) to be applied to ALL unlisted countries -- a side effect the user did not request. To actually remove a country from IEEPA, the user must use set_country_scope(exclude=...), not set_rate(rate=0). This is a subtle footgun, though numerically benign for the zeroed country itself (rate_ieepa_recip = 0 for it).
- **evidence:** PROBE 1 (run, exit 0): set_rate(ieepa_reciprocal, country='9999', rate=0) -> by_country['9999']=0, length(by_country)=3, has_active_ieepa would be length>0 = TRUE. Code: scenario_ops.R:411-412 `bc <- .nz(pr$by_country, TRUE); for (c in country) bc[[c]] <- rate` (no special-case for rate==0). Gate: 06:942 `has_active_ieepa <- !ieepa_invalidated && length(recip_by_country) > 0`.
- **suggested fix (NOT applied):** If a 0 rate is meant to remove the country, delete the by_country entry (and the parallel companion-map entries) when rate==0; otherwise document that set_rate cannot turn a country OFF and direct users to set_country_scope(exclude=...). Optionally warn when a set_rate(0) flips a previously-off gate ON.
- **verifier:** Independently verified against the real code. scenario_ops.R is a NEW file on theseus (git diff 7779933...theseus shows `new file mode 100644`; `git show master:src/scenario_ops.R` errors "exists on disk, but not in master"), so this is entirely refactor-introduced code with no prior behavior to compare against — `introduced_by_refactor: true` is accurate.

The cited mechanism is real and matches the finding's snippet exactly:
- src/scenario_ops.R:411-412 — `bc <- .nz(pr$by_country, TRUE); for (c in country) bc[[c]] <- rate`. There is NO special-case for rate==0, so set_rate(country, 0) writes

### [LOW] set_rate accepts negative rates for section_232 and section_122 (IEEPA rejects them) — input-validation inconsistency
_Counterfactual scenario verbs · refactor · verdict=confirmed · finder-confidence=medium_
- **file:** `src/scenario_ops.R` scenario_ops.R:393-396 (shared numeric guard), 406 (IEEPA-only rate<0 guard), 429-457 (122/232 paths have no >=0 check)
- **problem:** The shared set_rate guard checks is.numeric/length 1/!is.na (lines 394-396) but not sign. The IEEPA path adds `if (rate < 0) stop(...)` (line 406), but the section_122 and section_232 paths do not. A negative rate therefore writes a negative rate$default for 122 or a 232 program. Effects are mostly masked (has_232 uses isTRUE(x>0) -> a negative rate leaves/sets the gate OFF; resolve_heading_rate only returns v if v>0), but s232_blanket_metal_rate and s122's value>0 gate would read the negative default, producing a negative additional duty for an explicitly-set negative metal rate. Nonsensical input, but the inconsistency (IEEPA guarded, 232/122 not) is a latent footgun.
- **evidence:** scenario_ops.R:406 `if (rate < 0) stop(sprintf('operation[%s] (set_rate): IEEPA rate must be >= 0', idx))` exists ONLY inside the IEEPA branch. The 122 branch (429-432) and 232 branch (438-456) write rate$default with no sign check.
- **suggested fix (NOT applied):** Hoist a `if (rate < 0) stop(...)` into the shared guard near lines 394-396 so all categories reject negative rates uniformly (or document why 232/122 intentionally allow them).
- **verifier:** Verified against the real code in /nfs/roberts/project/pi_nrs36/jar335/Repositories/tariff-rate-tracker/src/scenario_ops.R and the calculator in src/06_calculate_rates.R.

CODE CITATION ACCURATE. The shared set_rate guard (scenario_ops.R:394-396) checks is.numeric/length-1/!is.na but NOT sign. The only `rate < 0` rejection is scenario_ops.R:406, inside the `if (auth %in% IEEPA_RATE_AUTHORITIES)` branch. The section_122 path (429-432) and section_232 path (438-456) write rate$default with no sign check. apply_operations (scenario_ops.R:138) only calls validate_spec_set (spec-structure validatio

### [LOW] Effectively-dead 'fall back to autos rate' arm in resolve_heading_rate (carried over from master)
_dead-code / vestigial / completeness / missing-things · pre-existing · verdict=confirmed · finder-confidence=high_
- **file:** `src/06_calculate_rates.R` 279
- **problem:** resolve_heading_rate ends with `cfg$default_rate %||% s232_spec_rate(specs, 'autos')`. For headings NOT in HEADING_RESOLVED_PROGRAM (auto_parts, buses, wood_furniture, kitchen_cabinets) the resolved rate comes from cfg$default_rate, and only if that is NULL does it inherit the autos program rate. Every such heading config sets default_rate in policy_params.yaml, so the autos fallback never executes — it is a dead arm. Same latent foot-gun as line 1574 (a non-auto heading lacking default_rate would silently get the autos rate). The pattern is inherited from master (line 1354: cfg$default_rate %||% s232_rates$auto_rate); the refactor translated the literal blob read to s232_spec_rate(specs,'autos'), semantically equivalent in baseline.
- **evidence:** 06_calculate_rates.R:279 'cfg$default_rate %||% s232_spec_rate(specs, "autos")'. Master equivalent git show 7779933:src/06_calculate_rates.R:1354 'rate = cfg$default_rate %||% s232_rates$auto_rate'. All heading configs in policy_params.yaml set default_rate (grep shows default_rate at every heading block).
- **suggested fix (NOT applied):** Drop the autos fallback and stop() if an unmapped heading has no default_rate, making the contract explicit; or comment why autos is the universal §232 default.
- **verifier:** Independently verified against the real code, not the finding's snippet.

(1) Line 279 of src/06_calculate_rates.R is exactly `cfg$default_rate %||% s232_spec_rate(specs, 'autos')`, inside resolve_heading_rate. HEADING_RESOLVED_PROGRAM (lines 254-262) maps only autos_passenger, autos_light_trucks, copper, softwood, mhd_vehicles, mhd_parts, semiconductors, pharmaceuticals — so for auto_parts, buses, wood_furniture, kitchen_cabinets the spec-mapped early-return is skipped and the final `%||%` arm decides the rate. Finding's description of the control flow is accurate.

(2) The autos fallback is 

### [LOW] run_comparisons.R survives as a vestigial stub (charter expected it deleted; --etrs branch is unimplemented, --tpc depends on deleted data/tpc/)
_dead-code / vestigial / completeness / missing-things · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/run_comparisons.R` 203-209, 41-59, 88
- **problem:** run_comparisons.R was NOT deleted on theseus (charter premise was wrong). It is now largely vestigial: its --etrs branch only prints 'Tariff-ETRs cross-repo comparison is not implemented in run_comparisons.R yet' (203-206), and its --tpc branch depends on data/tpc/tariff_by_flow_day.csv (data/tpc/ was deleted on theseus — only tpc_notes.txt was tracked; the CSV was always external). No build/submit script invokes it. It does NOT crash (file.exists guards at 55 and tpc_available at 182), but it is dead-but-shipped code. Its master version source()'d the now-deleted 08_weighted_etr.R at line 184 — that orphan WAS cleaned in the rewrite, so no runtime break.
- **evidence:** run_comparisons.R:205 'Tariff-ETRs cross-repo comparison is not implemented in run_comparisons.R yet.'; line 88 still source()s 07_validate_tpc.R; grep -rln run_comparisons across scripts/ src/ returns only the file itself. Master git show 7779933:src/run_comparisons.R:184 had source(08_weighted_etr.R), now removed.
- **suggested fix (NOT applied):** Either delete run_comparisons.R (and the standalone-main blocks in 07_validate_tpc.R/diagnostics.R that still default tpc_path='data/tpc/...') as part of the ETR/TPC vestige cleanup, or restore data/tpc provisioning and wire it into a submit script so it is actually exercised.
- **verifier:** Independently verified against master (7779933) and theseus, reading the actual files rather than the finding's snippet.

Confirmed facts:
1. run_comparisons.R was NOT deleted on theseus — `git diff 7779933...theseus -- src/run_comparisons.R` shows only 39 deletions (the ETR blocks), file survives at 216 lines.
2. The --etrs branch (current lines 203-209) only prints "Tariff-ETRs cross-repo comparison is not implemented in run_comparisons.R yet." — verbatim at line 205. Unimplemented, confirmed.
3. data/tpc/ is entirely gone on theseus: `git ls-tree -r theseus -- data/tpc/` returns nothing; ma

### [LOW] Stale docstring: run_alternative_series claims a 'TPC stacking alternative (tpc_additive)' it no longer performs
_dead-code / vestigial / completeness / missing-things · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/09_daily_series.R` 1526
- **problem:** The run_alternative_series header comment states 'TPC stacking alternative: re-applies stacking with tpc_additive method.' The function body (1543-1613) no longer re-applies stacking with tpc_additive — that logic was part of the removed legacy post-build scenario engine; the function now drives only the USMCA-share rebuild alternatives. The tpc_additive CAPABILITY itself is still alive in stacking.R/build_daily_aggregates, so this is purely a stale comment describing behavior the function dropped, which can mislead a reader into thinking a tpc_additive alternative series is still produced here.
- **evidence:** 09_daily_series.R:1526 docstring vs body 1543-1613 (sed of 1543-1613 finds no tpc_additive / re-stack). The legacy-engine removal note is at 1554-1559.
- **suggested fix (NOT applied):** Delete the 'TPC stacking alternative' line from the run_alternative_series docstring (or, if a tpc_additive alternative is still wanted as published output, add it to build_rebuild_alt_registry).
- **verifier:** Verified directly against the current theseus source and git history.

CONFIRMED FACTS:
1. /nfs/roberts/project/pi_nrs36/jar335/Repositories/tariff-rate-tracker/src/09_daily_series.R line 1526 contains the exact docstring: "#' TPC stacking alternative: re-applies stacking with 'tpc_additive' method."
2. The function body run_alternative_series (lines 1543-1613) contains ZERO references to tpc_additive, re-stacking, or stacking (grep over 1543-1613 returned no matches). The only other tpc_additive mention in the whole file is an unrelated guard comment at line 224.
3. The body now drives ONLY t

### [LOW] Unknown-232-program guards throw a cryptic 'subscript out of bounds' instead of the intended helpful error
_Counterfactual scenario verbs · refactor · verdict=confirmed · finder-confidence=medium_
- **file:** `src/scenario_ops.R` 443-445, 474-476
- **problem:** S232_RATE_FIELD and S232_EXEMPT_FIELD are atomic NAMED CHARACTER vectors (lines 77-83). The guards `if (is.null(S232_RATE_FIELD[[prog]])) stop(...helpful...)` and `if (is.null(S232_EXEMPT_FIELD[[prog]])) stop(...helpful...)` rely on `[[` returning NULL for an unknown name — but on an atomic vector `[[` with a non-matching character index throws 'subscript out of bounds', it does NOT return NULL (that is list-only semantics). So for an unknown/unsupported program the code errors at the indexing step with an opaque message, and the carefully-worded 'unknown section_232 program "x" (have: ...)' / 'no exemption list for ... (have: ...)' messages are effectively dead code. It still fails loud (good), but with a confusing error; the test (program='unobtanium' / 'copper') passes only because any error satisfies expect_error.
- **evidence:** scenario_ops.R:77 `S232_RATE_FIELD <- c(steel = 'steel_rate', ...)` (atomic). scenario_ops.R:443 `if (is.null(S232_RATE_FIELD[[prog]])) stop(...)` ; scenario_ops.R:474 same for S232_EXEMPT_FIELD. In R, `c(a='x')[['z']]` -> Error: subscript out of bounds (NOT NULL), so the is.null() branch is never reached for an unknown prog.
- **suggested fix (NOT applied):** Use name-membership tests instead of `[[`: `if (!prog %in% names(S232_RATE_FIELD)) stop(...)` and `if (!prog %in% names(S232_EXEMPT_FIELD)) stop(...)`. This restores the intended helpful error message.
- **verifier:** Every claim verified independently and empirically.

1) Vectors are atomic: src/scenario_ops.R:77-83 define S232_RATE_FIELD and S232_EXEMPT_FIELD via c(name='field', ...) — atomic NAMED character vectors, not lists.

2) Guards as cited: line 443 `if (is.null(S232_RATE_FIELD[[prog]])) stop(...'unknown section_232 program "%s" (have: %s)'...)` and line 474 `if (is.null(S232_EXEMPT_FIELD[[prog]])) stop(...'no exemption list for section_232 program "%s" (have: %s)'...)` — verbatim match.

3) R semantics empirically confirmed (R 4.4.2-gfbf-2024a-bare): `c(steel='steel_rate',...)[["unobtanium"]]` th

### [LOW] add_program drops every rate layer except flat and ignores prog$stacking/metal/active
_Counterfactual scenario verbs · refactor · verdict=confirmed · finder-confidence=medium_
- **file:** `src/scenario_ops.R` 528-534
- **problem:** op_add_program constructs the new program with rate = list(flat = flat), silently discarding any other rate layers a caller passed in prog$rate (e.g. a default/by_country/overrides), and it never forwards prog$stacking, prog$metal, or prog$active to authority_program(). A user supplying rate = list(flat = 0.5, by_country = c(...)) or a per-program stacking class would have those fields silently dropped — the new program rides only the flat rate on rate_other with the authority's default stacking. Since the no-Ch99 seeder only consumes flat, this is currently latent, but it is a silent contract narrowing with no warning.
- **evidence:** scenario_ops.R:528-533 `newp <- authority_program(id=id, product_scope=..., country_scope=..., rate = list(flat = flat))` — only flat is forwarded; prog$rate's other keys, prog$stacking, prog$metal, prog$active are never read.
- **suggested fix (NOT applied):** Either forward prog$stacking/prog$metal/prog$active and the full prog$rate (validated), or fail loud if prog$rate contains keys other than flat / if unsupported fields are present, so the narrowing is explicit rather than silent.
- **verifier:** Verified against the real code at /nfs/roberts/project/pi_nrs36/jar335/Repositories/tariff-rate-tracker/src/scenario_ops.R:528-533. op_add_program constructs the new program as authority_program(id=..., product_scope=..., country_scope=..., rate = list(flat = flat)). This reads ONLY prog$rate$flat (lines 520-523) and forwards ONLY flat. A grep over the whole file finds zero references to prog$stacking, prog$metal, prog$active, or any non-flat prog$rate key, yet authority_program() (src/authority_spec.R:55-62) explicitly accepts metal, active, and stacking params. So those caller-supplied field

### [LOW] validate_rate does not reject duplicate lookup keys in by_country / by_product_tier (silent first-wins at read time)
_The generic engine · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/authority_spec.R` 316-324
- **problem:** chk_named_num only checks that names are present and non-empty and values are finite/non-negative; it never checks for DUPLICATE names. A spec with `by_country = c(`5700`=0.50, `5700`=0.30)` validates cleanly, but resolve_rate's `key %in% names(bc)` + `bc[[key]]` (line 238-239) silently returns the FIRST match (0.50), discarding the second. Likewise for by_product_tier. Because the adapter constructs these maps, a duplicate would normally indicate an upstream collapse bug (e.g. a missing group_by/max). A weak validator lets that bug ride silently into a first-wins read rather than failing loud. Same gap exists for by_country_eo_rate.
- **evidence:** `chk_named_num <- function(x, nm) { ... if (is.null(names(x)) || any(!nzchar(names(x)))) stop(...); vals <- as.numeric(unlist(x)); if (... any(vals < 0)) stop(...) }` — no `any(duplicated(names(x)))` check. resolve_rate read: `key <- as.character(country); if (key %in% names(bc)) return(desc(bc[[key]], 'by_country'))` returns first.
- **suggested fix (NOT applied):** Add `if (any(duplicated(names(x)))) stop(sprintf('[%s] rate$%s has duplicate keys: %s', ctx, nm, ...))` inside chk_named_num (and the by_country_type / by_country_eo_* checks).
- **verifier:** Independently verified against the real code (authority_spec.R does not exist on master; it is entirely new in the theseus refactor).

CODE CLAIMS — all accurate as quoted:
- chk_named_num (src/authority_spec.R:316-324) validates: names present + all nzchar (319-320) and values finite/non-negative (321-323). It has NO any(duplicated(names(x))) check. Verified verbatim.
- resolve_rate by_country read (237-239): `key <- as.character(country); if (key %in% names(bc)) return(desc(bc[[key]], 'by_country'))`. R's `[[` with a character index returns the FIRST element whose name matches — deterministi

### [LOW] validate_rate ignores unnamed (empty-name) stray rate-list elements instead of rejecting them
_The generic engine · refactor · verdict=confirmed · finder-confidence=medium_
- **file:** `src/authority_spec.R` 300-303
- **problem:** The unknown-field check filters to non-empty names before erroring: `unknown <- setdiff(names(rate), allowed); unknown <- unknown[nzchar(unknown)]; if (length(unknown)) stop(...)`. A stray POSITIONAL (unnamed) element in the rate list has name '' (or rate has no names attribute at all), so it is dropped by the nzchar filter and never reported. e.g. `list(0.5, default = 0.1)` validates; the `0.5` is silently ignored by both validator and resolve_rate. The nzchar filter is needed to tolerate the entry-form overrides list (which is validated separately), but at the top-level `rate` it lets authoring typos (a value with no key) pass silently.
- **evidence:** `unknown <- setdiff(names(rate), allowed); unknown <- unknown[nzchar(unknown)]` — empty-string names are removed before the `if (length(unknown)) stop(...)`. A rate with a positional element therefore passes.
- **suggested fix (NOT applied):** At the top-level rate, require every element to be named: `if (is.null(names(rate)) || any(!nzchar(names(rate)))) stop(sprintf('[%s] rate must be a fully-named list', ctx))` before the allowed-keys diff (the per-entry overrides/floors lists already validate their own positional shape separately).
- **verifier:** Verified against the real code at /nfs/roberts/project/pi_nrs36/jar335/Repositories/tariff-rate-tracker/src/authority_spec.R lines 300-303. The file is new in the theseus refactor (it does not exist in master: `git show master:src/authority_spec.R` -> "exists on disk, but not in 'master'"), so this is refactor-introduced. The cited lines match the finding verbatim: `unknown <- setdiff(names(rate), allowed); unknown <- unknown[nzchar(unknown)]; if (length(unknown)) stop(...)`.

Mechanism confirmed empirically with R 4.4.2: for `list(0.5, default = 0.1)`, `names()` = c("", "default"); `setdiff(.

### [LOW] new_coverage.R declares a runtime dependency on authority_spec.R that helpers.R does not source (header 'Sourced via helpers.R' is misleading)
_The generic engine · refactor · verdict=confirmed · finder-confidence=medium_
- **file:** `src/new_coverage.R` 16-18, 84-103
- **problem:** apply_new_coverage_programs / its header rely on `resolve_country_scope` and `%||%` from authority_spec.R (header line 16: 'Depends on src/authority_spec.R (resolve_country_scope, %||%) ... Sourced via helpers.R'). But helpers.R sources rate_schema.R and new_coverage.R (helpers.R:17-18) and does NOT source authority_spec.R. Production works only because 00_build_timeseries.R sources authority_spec.R (line 62) before any call, and R resolves these symbols lazily at call time. Any consumer that sources only helpers.R and then calls apply_new_coverage_programs would fail with 'could not find function resolve_country_scope'. The dependency is real but unenforced; the 'Sourced via helpers.R' phrasing implies helpers.R provides the full dependency closure, which it does not.
- **evidence:** helpers.R sources: policy_params, revisions, stacking, resolved_programs, timeline, rate_schema, new_coverage, data_loaders, output_paths — no authority_spec.R. new_coverage.R apply_new_coverage_programs calls resolve_country_scope(p$country_scope ..., countries) at line 91 and uses %||% throughout.
- **suggested fix (NOT applied):** Either source authority_spec.R from helpers.R before new_coverage.R, or correct the new_coverage.R header to state that authority_spec.R must be sourced separately by the caller (as 00_build_timeseries.R does).
- **verifier:** Independently verified against /nfs/roberts/project/pi_nrs36/jar335/Repositories/tariff-rate-tracker on theseus (new_coverage.R does not exist on master; it is refactor-introduced).

CORE CLAIM CONFIRMED. The header of src/new_coverage.R lines 16-17 reads: "Depends on src/authority_spec.R (resolve_country_scope, %||%) and src/rate_schema.R (add_blanket_pairs). Sourced via helpers.R." But src/helpers.R (lines 12-20) sources policy_params, revisions, stacking, resolved_programs, timeline, rate_schema, new_coverage, data_loaders, output_paths — and NOT authority_spec.R. resolve_country_scope is d

### [LOW] op_add_program accepts flat=0 but collect_seeded_programs silently drops it (zero-coverage no-op)
_The generic engine · refactor · verdict=confirmed · finder-confidence=medium_
- **file:** `src/new_coverage.R` 62-66
- **problem:** collect_seeded_programs only collects programs whose flat rate is non-zero: `if (!is.null(flat) && is.numeric(flat) && length(flat) == 1L && !is.na(flat) && flat != 0)`. But op_add_program (scenario_ops.R:520-522) only rejects flat that is NULL/non-numeric/non-scalar/NA — it ACCEPTS flat=0. So a scenario can author an add_program with flat=0, pass validation, and have it silently contribute nothing (the program exists on the spec but is invisible to the seeder). This is a quiet inconsistency between the authoring contract (0 allowed) and the application contract (0 ignored). Minor because a 0% new tariff is economically a no-op, but it can mask an authoring error (intended a real rate, mistyped 0).
- **evidence:** collect_seeded_programs gate includes `&& flat != 0`; op_add_program guard is `if (is.null(flat) || !is.numeric(flat) || length(flat) != 1L || is.na(flat)) stop(...)` — 0 passes. Tested baseline relies on this filter to stay dormant, but flat=0 from a scenario is a silent drop.
- **suggested fix (NOT applied):** Either reject flat==0 in op_add_program (a no-op program is almost certainly an error) or emit a message() in apply_new_coverage_programs/collect_seeded_programs when a flat==0 program is encountered and skipped.
- **verifier:** Verified independently against the actual code on theseus. (1) The cited gate is real: src/new_coverage.R:63 reads `if (!is.null(flat) && is.numeric(flat) && length(flat) == 1L && !is.na(flat) && flat != 0)` — the trailing `&& flat != 0` filters out zero-rate programs. (2) op_add_program (src/scenario_ops.R:520-523) guards only against NULL/non-numeric/non-scalar/NA: `if (is.null(flat) || !is.numeric(flat) || length(flat) != 1L || is.na(flat)) stop(...)`; flat=0 passes all four checks and the program is appended to the spec at line 534. So the authoring contract (0 allowed) and the application

### [LOW] gz-archive awareness added to forward availability check but NOT to the reverse 'JSON without CSV entry' check in 01_scrape_revision_dates.R
_master-fix-port + revision-re-dating · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/01_scrape_revision_dates.R` 356-374
- **problem:** theseus made the forward availability scan gz-aware (checks both .json.gz and .json at lines 358-363) but left the reverse drift check — 'JSON files without CSV entry' — pattern-matching only raw .json: list.files(archive_dir, pattern = 'hts_\\d{4}_.*\\.json$') at line 367. Since archives are committed gzipped (.json.gz), any gzipped archive present on disk but missing from revision_dates.csv will silently NOT be reported as missing_csv. This is an asymmetry the original master code did not have (master used plain .json for BOTH checks, so both were consistently gz-blind).
- **evidence:** Line 367: json_files <- list.files(archive_dir, pattern = 'hts_\\d{4}_.*\\.json$')  — the '$' anchors on .json with no optional (\\.gz)? group, unlike the gz-aware list_available_revisions() pattern 'hts_', year, '.*\\.json(\\.gz)?$' in revisions.R:153. The forward check at lines 359-363 WAS updated: file.exists(paste0(base, '.json.gz')) || file.exists(paste0(base, '.json')).
- **suggested fix (NOT applied):** Make the reverse check gz-aware too: pattern = 'hts_\\d{4}_.*\\.json(\\.gz)?$' at line 367 and strip an optional trailing .gz in the str_match at line 369 (e.g. 'hts_(\\d{4})_(.*)\\.json(?:\\.gz)?$'). Low impact: this block lives in the if (sys.nframe() == 0) standalone-scraper path and is not in the daily build (00_build_timeseries.R sources 04/05 but not this scrape entrypoint); it only under-reports CSV/archive drift in a diagnostic.
- **verifier:** Independently verified against master, the theseus diff, and the current file. All load-bearing claims hold.

CONFIRMED FACTS:
1. Forward availability check WAS made gz-aware in theseus. The diff 7779933...theseus shows lines 356-360 changed from `json_name <- paste0('hts_', parsed$year, '_', parsed$rev, '.json')` + `file.exists(...)` to `base <- ...` + `file.exists(paste0(base, '.json.gz')) || file.exists(paste0(base, '.json'))`.
2. The reverse 'JSON without CSV entry' check at line 367 was NOT updated: it still uses `list.files(archive_dir, pattern = 'hts_\\d{4}_.*\\.json$')` and the str_mat

### [LOW] fix2+7 policy_params.yaml comment update not ported (functional config keys ARE present)
_master-fix-port + revision-re-dating · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `config/policy_params.yaml` 620-626
- **problem:** origin/master's fix2+7 commit (c471770) updated the explanatory comment above ieepa_exempt_scope to describe the new Annex-II inheritance path ('plus Annex II inheritance for EOs in country_eo_annex_ii_inherit (below) and the standard ch98 claim exemption'). The theseus port (70b6b97) added the functional config keys (country_eo_annex_ii_inherit: ['9903.01.84'] at line 708, applicability_shares_file at line 147) but did NOT update this comment block, so theseus's comment still reads the stale 'always use the SEPARATE per-EO list ... regardless of this setting'. The comment now under-describes the actual behavior.
- **evidence:** diff c471770:policy_params.yaml vs 70b6b97:policy_params.yaml shows the comment hunk (lines starting '# always use the SEPARATE...' replaced by '# use the SEPARATE... plus Annex II inheritance...') present only on master. theseus:policy_params.yaml lines 620-626 retain the old wording, while the functional key country_eo_annex_ii_inherit: ['9903.01.84'] IS present (line 708) and consumed in src/06_calculate_rates.R:1194,1218,1289.
- **suggested fix (NOT applied):** Port the 3-line comment update to keep the config self-documenting. No behavior impact — purely documentation, since the consuming code and config value were ported correctly and the India 3004 Annex-II inheritance is functionally live.
- **verifier:** Confirmed as a low-severity doc-mismatch with no functional/parity impact.

Ancestry note that initially looked like it refuted the finding: local `master` (7779933) is BEHIND origin/master and still has the stale comment with no Annex-II key. But the finding explicitly references "origin/master's fix2+7 commit (c471770)". c471770 IS an ancestor of origin/master (5cb951d), not of local master — so the correct comparison is origin/master, which the finding got right.

Verified facts:
1. origin/master:config/policy_params.yaml lines 619-627 carry the UPDATED comment from c471770: "# use the SEPA

### [LOW] tpc_additive total_additional drops rate_301_cs (asymmetric with the mutual_exclusion path)
_Stacking Generalization · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/stacking.R` 279-288
- **problem:** The tpc_additive branch of apply_stacking_rules sums rate_232 + rate_ieepa_recip + rate_ieepa_fent + rate_301 + rate_s122 + rate_section_201 + rate_other — it OMITS rate_301_cs. The default mutual_exclusion path includes rate_301_cs (it is in default_stacking_policy / stacking_policy_from_specs). So the two stacking methods disagree the moment rate_301_cs is non-zero (the A2 content-split-301 feature, enabled by config section_301_content_split_codes). Baseline-safe only because rate_301_cs is all-zero in baseline (+0).
- **evidence:** stacking.R:283-284 `total_additional = rate_232 + rate_ieepa_recip + rate_ieepa_fent + rate_301 + rate_s122 + rate_section_201 + rate_other` — no rate_301_cs term. Contrast the mutual_exclusion path (stacking.R:290-297) which sums over the policy whose order includes rate_301_cs. master had no rate_301_cs at all, so the omission is a theseus-introduced asymmetry.
- **suggested fix (NOT applied):** Add `+ rate_301_cs` to the tpc_additive total (and net_301_cs = rate_301_cs to the tpc branch of compute_net_authority_contributions) so the sensitivity path stays consistent with mutual_exclusion when A2 is enabled; or guard tpc_additive against a non-empty rate_301_cs.
- **verifier:** CONFIRMED with concrete evidence.

The cited code does what the finding claims. In /nfs/roberts/project/pi_nrs36/jar335/Repositories/tariff-rate-tracker/src/stacking.R the tpc_additive branch (current lines 285-294; finding cited 279-288/283-284, slightly stale but substantively correct) computes:
  total_additional = rate_232 + rate_ieepa_recip + rate_ieepa_fent + rate_301 + rate_s122 + rate_section_201 + rate_other
— rate_301_cs is genuinely OMITTED.

The asymmetry is real. The mutual_exclusion path builds total_additional from the policy (default_stacking_policy / stacking_policy_from_specs

### [LOW] 09 daily series re-stack is specs-less: set_stacking and re-scoped-301 counterfactuals are reverted/invisible in daily + weighted-ETR outputs
_Stacking Generalization · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/09_daily_series.R` 131,155,173,192,220-247,267,290,557
- **problem:** Every 09 call to apply_stacking_rules / compute_net_authority_contributions passes NO stacking_policy, so they fall back to default_stacking_policy(). 09 RE-STACKS the persisted rate_* (lines 142/272 read total_additional AFTER the line-131/267 re-stack), OVERWRITING the spec-driven total 06 persisted. For a set_stacking counterfactual (e.g. flip section_301 to content_split, or edit fentanyl exceptions, or a re-scoped-301 to Vietnam handled additively), 06's snapshot reflects it but the daily aggregate silently reverts to the default stacking — so mean_232/mean_301/.../etr_* and the expanded daily panel's total_additional do NOT reflect the scenario. Baseline is byte-identical (default policy == baseline spec policy), so the parity gate cannot see this.
- **evidence:** 09_daily_series.R:131 `apply_stacking_rules(rev_data, stacking_method=stacking_method)` (no stacking_policy arg) and :220 `compute_net_authority_contributions(rev_data, cty_china=CTY_CHINA, stacking_method=stacking_method)` (no policy). scenario_ops.R:355-360 documents the gap honestly ('the 09 daily aggregate RE-STACK reads the PERSISTED rate_* columns with NO specs ... a set_stacking counterfactual is NOT seen'). Master 09 was identical (hardcoded case_when), so this is a generality gap carried into a calculator that now CLAIMS spec-driven stacking, not a baseline regression.
- **suggested fix (NOT applied):** Thread the spec-derived policy (or persist the policy with the snapshot) into the 09 re-stack calls so a set_stacking / additive-301-rescope scenario flows into the daily and weighted-ETR decomposition; this is the documented Pass-2 item.
- **verifier:** Independently verified via git diff 7779933...theseus, git show master:src/09_daily_series.R, and reading the live files. Every load-bearing claim holds:

1. 09's re-stack is specs-less. /nfs/roberts/project/pi_nrs36/jar335/Repositories/tariff-rate-tracker/src/09_daily_series.R:131,155,173,192,220,243,267,290 all call apply_stacking_rules / compute_net_authority_contributions with NO stacking_policy arg. src/stacking.R:296 and :350 fall back to default_stacking_policy(cty_china) when stacking_policy is NULL.

2. 09 RE-STACKS and overwrites the persisted total. apply_stacking_rules RECOMPUTES t

### [LOW] Daily expanded output omits rate_301_cs from default_columns (component invisible when A2 is enabled)
_Stacking Generalization · refactor · verdict=confirmed · finder-confidence=medium_
- **file:** `src/09_daily_series.R` 561-564
- **problem:** rate_301_cs is a REQUIRED_RATE_COLS member (06:722), computed and persisted, and contributes to total_additional. But the daily expanded output default_columns (561-564) lists rate_232, rate_301, rate_ieepa_recip, rate_ieepa_fent, rate_s122, rate_section_201, rate_other — NOT rate_301_cs. So when section_301_content_split_codes is non-empty, the per-row daily panel carries a correct total_additional but cannot expose the content-split-301 component; a downstream consumer sees a total it cannot decompose. total_additional itself is correct (default policy includes rate_301_cs), so this is output-completeness, not a numeric bug.
- **evidence:** 09_daily_series.R:562-563 default_columns has 'rate_section_201','rate_other' but no 'rate_301_cs'. 06:720-722 REQUIRED_RATE_COLS includes 'rate_301_cs'. The aggregate path (232) reports net_301_cs UNDER net_301, but the expanded per-row path drops the column.
- **suggested fix (NOT applied):** Add 'rate_301_cs' to 09 default_columns (and ensure the expanded path carries it) so the content-split-301 component is visible in the daily panel when A2 is active.
- **verifier:** Independently verified against src/09_daily_series.R (theseus), git show master:src/09_daily_series.R, src/06_calculate_rates.R, and src/stacking.R.

The substantive mechanism is REAL and confirmed:
- src/09_daily_series.R:561-564: default_columns lists rate_232, rate_301, rate_ieepa_recip, rate_ieepa_fent, rate_s122, rate_section_201, rate_other but NOT rate_301_cs. Verified by reading the actual lines (finding's cited line numbers are slightly off — the block is at 561-564 in theseus, but the content matches exactly).
- rate_301_cs IS a REQUIRED_RATE_COL (06:788), is persisted on the interva

### [LOW] Floor-exemption static fallback is applied date-blind across all revisions
_timing / date-gating / cross-revision leakage · pre-existing · verdict=confirmed · finder-confidence=medium_
- **file:** `src/data_loaders.R` 93-107 (load_revision_floor_exemptions); called at src/authority_adapter.R:618
- **problem:** The adapter bakes the IEEPA floor exemptions via load_revision_floor_exemptions(revision_id), which tries a per-revision file data/us_notes/floor_exempt_{revision}.csv and otherwise returns the static resources/floor_exempt_products.csv unchanged. The static fallback carries no effective_date columns, so any revision lacking a per-revision file gets the CURRENT static floor-exempt set regardless of when each EU/JP/KR/Swiss floor-exemption category actually took legal effect — a potential cross-revision leak for revisions that fall back. This is the 'un-versioned side-data' soft spot the audit memory flagged. It is NOT refactor-introduced: master's calc (origin/master:06:859) called the identical loader, so the adapter relocation preserves the existing behavior exactly.
- **evidence:** data_loaders.R:103-106 fall-through: `message('  No per-revision floor exemptions...; using static fallback'); return(load_floor_exempt_products())`; load_floor_exempt_products (72-83) read_csv with no date filter. resources/floor_exempt_products.csv has no effective_date column. origin/master:src/06_calculate_rates.R also uses load_revision_floor_exemptions(revision_id) (line 859).
- **suggested fix (NOT applied):** Audit which revisions actually rely on the static fallback (vs. a per-revision floor_exempt_{rev}.csv); for those, either ship per-revision files or add effective_date_start/_end columns to the static file and date-gate it the same way the universal IEEPA list is gated, so floor exemptions cannot apply before their legal effective date.
- **verifier:** Independently verified every claim against the real code, not the finding's snippet.

(1) CODE MATCHES CLAIM: src/data_loaders.R:93-107 load_revision_floor_exemptions tries data/us_notes/floor_exempt_{revision}.csv and, on miss, message('...using static fallback') + return(load_floor_exempt_products()). load_floor_exempt_products (72-83) read_csv with no date filter. CONFIRMED.

(2) STATIC FILE HAS NO DATE COLUMN: resources/floor_exempt_products.csv header is exactly `hts8,category,country_group,ch99_code` (3823 data rows). No effective_date. CONFIRMED.

(3) NOT REFACTOR-INTRODUCED / PARITY-NE

### [LOW] run_daily_series(snapshot_dir=) stops on a missing/stale daily-parts cache and never falls back to build_daily_aggregates_streaming — contradicting the plan doc; _streaming is dead on the gather path
_PUBLISH / DAILY-AGGREGATION / PARITY-TOOLING INTEGRITY · refactor · verdict=confirmed · finder-confidence=high_
- **file:** `src/09_daily_series.R` 1082-1098
- **problem:** The production gather (scripts/build_gather.R:182) calls run_daily_series(snapshot_dir=output_dir,...). That branch calls load_daily_parts_if_complete() and, if it returns NULL (parts missing/stale/wrong-weight-mode), executes stop(...) — it does NOT call build_daily_aggregates_streaming(). The plan doc states the opposite: 'stale/missing parts ... fall back to the existing snapshot-streaming path' (docs/spec_driven_calculator_plan.md:784-786). Because no caller routes a missing-parts gather to build_daily_aggregates_streaming(), that function is effectively dead code on the production gather path (only tests/docs reference it). This is fail-loud (no silent wrong numbers), so the impact is low, but the doc claim is false and the streaming fallback the doc relies on for resilience does not exist.
- **evidence:** src/09_daily_series.R:1088-1092 `if (is.null(daily)) { stop('Daily aggregate parts are missing or stale for ', snapshot_dir, '. Re-run the array build ...') }` — no call to build_daily_aggregates_streaming in this path. build_gather.R:176-178 comment correctly says 'missing/stale parts fail the gather', but plan doc:784-786 says it falls back. build_daily_aggregates_streaming (09_daily_series.R:1001-1067) is only invoked from itself/tests, not run_daily_series.
- **suggested fix (NOT applied):** Either (a) make run_daily_series fall back to build_daily_aggregates_streaming(snapshot_dir, rev_dates, imports, policy_params) when the parts cache is incomplete (matching the doc and giving the gather a parts-independent path), or (b) update the plan doc to state that the gather hard-fails on an incomplete parts cache and delete/clearly-mark build_daily_aggregates_streaming as a non-production utility.
- **verifier:** Verified independently against the real code, the caller, the doc, and master.

CODE: /nfs/roberts/project/pi_nrs36/jar335/Repositories/tariff-rate-tracker/src/09_daily_series.R:1082-1092 — the snapshot_dir branch of run_daily_series() calls load_daily_parts_if_complete() and, when it returns NULL, executes stop('Daily aggregate parts are missing or stale ...'). There is no call to build_daily_aggregates_streaming() in that branch (or anywhere in run_daily_series). Confirmed exactly as the finding quotes.

CALLER: scripts/build_gather.R:182 calls run_daily_series(snapshot_dir = output_dir, ...

### [LOW] Weak/vacuous assertion: 'rate_301 only contributes for China' cannot catch a broken China-only gate
_TEST QUALITY / PARITY-BLINDNESS / COVERAGE GAPS · pre-existing · verdict=confirmed · finder-confidence=medium_
- **file:** `tests/test_rate_calculation.R` 411-430, 476-481
- **problem:** The invariant 'rate_301 only contributes for China in decomposition' (line 476) filters non-China rows and asserts net_301 == 0. But in the fixture make_test_rates() the rate_301 INPUT is nonzero only at the two China rows (line 418: rate_301 = c(0.25,0,0,0.25,0,0,0,0,0), with country pattern (5700,4280,1220) repeated — positions 1 and 4 are both 5700). No non-China row has a nonzero rate_301 input. So the assertion confirms only that an already-zero input stays zero; it would still pass even if compute_net_authority_contributions had no China gate at all (301 china-restriction actually comes from the rate being set to 0 upstream, not from the stacking policy — default policy rate_301 has class='additive' with no additive/country gating). The test does not stress the claimed invariant.
- **evidence:** make_test_rates() line 418 rate_301 nonzero only at China rows; src/stacking.R:147 default policy rate_301 = list(net='net_301', class='additive') carries no China gate. So the 'China-only' property is a function of input data, not of the code path the test name implies.
- **suggested fix (NOT applied):** Give a non-China row a nonzero rate_301 input in the fixture (or add a dedicated row) so that net_301==0 for non-China actually depends on a gate; if no such gate exists in the decomposition, the test name is misleading and should be reworded to reflect that 301 china-scoping is enforced upstream by zeroing the rate.
- **verifier:** CONFIRMED. I independently verified every load-bearing claim against the actual code (Rscript is unavailable on the login node, so verification is by direct code reading, which is conclusive here).

(1) The test exists and is unchanged by the refactor. tests/test_rate_calculation.R lines 411-430 (make_test_rates) and 476-481 ("rate_301 only contributes for China in decomposition") are byte-identical between `git show master:` and the current theseus file (the 32-line master-vs-current diff is entirely elsewhere; `git diff 7779933 theseus -- tests/test_rate_calculation.R` is empty). The finding

### [LOW] Relocated $exempt_products sets and the s232 rate$resolved blob are invisible to validate_authority_spec — no schema guard on relocated payloads
_TEST QUALITY / PARITY-BLINDNESS / COVERAGE GAPS · refactor · verdict=confirmed · finder-confidence=medium_
- **file:** `tests/test_authority_spec.R` 60-68
- **problem:** By design (documented), the Pass-1.5 product-exemption sets are baked onto a plain program-level $exempt_products field and the §232 residual blob onto rate$resolved, both deliberately INVISIBLE to validate_authority_spec / validate_rate (authority_adapter.R:305-309 and test assertions like test_exempt_sets_in_spec.R:151-152 'invisible to the validator'). The validator tests (test_authority_spec.R, test_resolve_rate.R) therefore provide no structural guard on these relocated policy inputs: a corrupted/empty/wrong-shape exempt set or resolved blob passes validation. Their only guards are (a) the oracle unit tests (good, but only assert the helper output, not that the build wired it in for every revision) and (b) data-gated parity/invariant. This is a deliberate tradeoff, but it means the 'validates (fail-loud passed inside)' assertion seen throughout the adapter tests is weaker than it appears for exactly the payloads the refactor relocated.
- **evidence:** test_exempt_sets_in_spec.R:151-152 'new $exempt_products field does not break validate_spec_set (invisible to validator)'; authority_adapter.R:305-309 comment 'invisible to validate_authority_spec, no schema change'. validate_authority_spec (src/authority_spec.R) validates rate via validate_rate but not $exempt_products or rate$resolved.
- **suggested fix (NOT applied):** Add light shape validation for the relocated payloads (e.g. exempt_products$universal is a character vector, $country_eo has cols {ch99_code, hts8_prefix}, $floor has {hts8, country_group}; rate$resolved$has_232 is logical) so a wiring/drain bug is caught at build time rather than only by a data-present invariant test.
- **verifier:** CONFIRMED, severity low (correct as filed). I independently verified the structural claim against the real code.

(1) `validate_authority_spec` (src/authority_spec.R:430-464) iterates programs and checks only stacking/usmca/metal then calls `validate_rate`; it never inspects the program-level `$exempt_products` field. `validate_rate` (281-419) lists 'resolved' in its `allowed` set (line 287) purely so it doesn't trip the unknown-field check, but applies NO structural check to its contents. test_resolve_rate.R:96-98,165-166 explicitly tests that the resolved blob is SKIPPED ('sentinels + resolv

## Uncertain / verdict-pending

### [LOW] The §301 cranes/chassis 'invariant' in test_timeline_invariants is self-referential (reads the same config the golden was minted from), so it is a parity-style tautology, not independent ground truth
_TEST QUALITY / PARITY-BLINDNESS / COVERAGE GAPS · refactor · verdict=uncertain · finder-confidence=high_
- **file:** `tests/test_timeline_invariants.R` 145-163
- **problem:** Line 145 sets `priced <- !is.null(pp$SECTION_301_RATES) && any(crane_codes %in% pp$SECTION_301_RATES$ch99_pattern)` and then, when priced, asserts the snapshot's max rate_301 for the crane/chassis hts8 `after == 1.0` (line 153). But the 1.00 value, the ch99 codes, and the covered hts8 ALL come from the same pp$SECTION_301_RATES + s301_product_lists.csv config that the build used to mint bnd_2026-11-10. So the test asserts 'the snapshot equals the config that produced it' — structurally identical to a parity check, which the doc itself says cannot catch a wrong number baked into the golden. If the priced rate were wrong (e.g. the hts8 over-application of .12 to all of 87163900, a judgment call the doc explicitly flags), this 'absolute invariant' would still be GREEN. It does not encode the statute-derived ground truth (100% from the Ch99 text) independently of the config.
- **evidence:** Lines 145-156: the threshold 1.0 and the crane_codes/cc_hts8 are config-derived, not an external citation. The doc (spec_driven_calculator_plan.md:659) records the judgment call 'hts8 over-application of .12 to all of 87163900' — exactly the kind of error a config-derived invariant cannot detect. Contrast with the genuinely-absolute Russia tests in test_rate_calculation.R:825-920 which cite the statute (Proc 10522, 200%) independently.
- **suggested fix (NOT applied):** Either hard-code the expected 1.00 and the exact covered hts8 set as statute-cited literals in the test (so a config drift fails), or downgrade the claim — this assertion is a parity check in invariant clothing for the §301 mint.
- **verifier:** The finding is partly right and partly overstated; it conflates two distinct dimensions (rate value vs. hts8 scope), so I cannot cleanly confirm or refute it as written.

VERIFIED FACTS (independent re-read):
- tests/test_timeline_invariants.R is NEW on theseus (does not exist on master — `git cat-file -e master:...` fails), so the master-vs-theseus parity-drift framing does not apply; this is a brand-new test.
- Cited lines 145-163 match the real file exactly. Line 145 `priced <- !is.null(pp$SECTION_301_RATES) && any(crane_codes %in% pp$SECTION_301_RATES$ch99_pattern)` IS config-derived. Line

### [MEDIUM] Swiss-framework turn-ON (effective 2025-11-14) is a mid-interval boundary that no discovery source covers — the floor override activates ~7 days late
_P2-1 Unified Timeline Splitter · pre-existing · verdict=(pending) · finder-confidence=medium_
- **file:** `src/timeline.R, config/policy_params.yaml, src/authority_adapter.R` timeline.R:57-86 (collect_schedule_boundaries) + 161-255 (discover_boundaries); policy_params.yaml:361-365 (swiss_framework); authority_adapter.R:232-236 (swiss_override_active gate)
- **problem:** P2-1 was built to make a tariff switch on its LEGAL effective date when that date falls strictly inside a revision interval (it fixed exactly this for the §232 metal-exemption expiry on 2025-03-12). But the Swiss/Liechtenstein framework's TURN-ON date (swiss_framework.effective_date = 2025-11-14) is never offered to discover_boundaries as a candidate. collect_schedule_boundaries() reads SWISS_FRAMEWORK only via boundary_from_expiry() (the EXPIRY 2026-03-31, which is then subtracted out for downstream zeroing); it never emits the effective_date. The Swiss override is also not an HTS Ch99 'on or after' offset (the per-revision scan found no offset at 2025-11-14), so the Ch99 scan can't see it either, and there is no §232-country-exemption or override entry for it. Net: 2025-11-14 falls strictly inside rev_29's interval ([2025-11-13, 2025-11-20] using policy dates: rev_29=2025-11-13, rev_30=2025-11-21) and is minted by nothing. The adapter gate swiss_override_active <- rev_date >= swiss_fw$effective_date is FALSE for rev_29 (stamped 11-13) and TRUE only from rev_30 (11-21), so the Swiss 15% floor override turns on at rev_30 (2025-11-21) instead of the legal 2025-11-14 — ~7 days late. For Switzerland the framework LOWERS the reciprocal rate to the 15% floor, so CH/LI carry the higher pre-deal rate_ieepa_recip for those extra days. The golden was re-frozen with this late activation baked in, so the numeric parity gate is blind to it.
- **evidence:** policy_params.yaml:362 `effective_date: '2025-11-14'`, :363 `expiry_date: '2026-03-31'`, :364 `finalized: false`. authority_adapter.R:235 `swiss_override_active <- rev_date >= swiss_fw$effective_date && (swiss_fw$finalized || rev_date <= swiss_fw$expiry_date)`. timeline.R:69-72 only adds SWISS via `boundary_from_expiry(pp$SWISS_FRAMEWORK$expiry_date)` (the expiry), never the effective_date. Empirically confirmed: collect_schedule_boundaries()={2026-02-20, 2026-04-01, 2026-07-24} — no 2025-11-14. Per-revision interior Ch99-offset scan over all 42 revisions returned only `2026_rev_9 [2026-05-01..2026-12-31] INTERIOR offsets: 2026-11-10` — i.e. no offset at 2025-11-14. revision_dates.csv: rev_29 policy_effective_date 2025-11-13, rev_30 2025-11-21, so 2025-11-14 is strictly interior to rev_29. Corroboration that this class of gap is real and known: rev_32's policy_event note states 'S. Korea floor (15%) ... legally retroactive to Nov 14 ... retro window not modeled (see todo)' — the same Nov-14 floor turn-on, flagged unmodeled for Korea but silently unaddressed for Switzerland.
- **suggested fix (NOT applied):** Add the Swiss (and any other floor-framework) effective_date as a first-class discover_boundaries source — analogous to the §232 country-exemption expiry source at timeline.R:231-237 — so a bnd_2025-11-14 snapshot (owner rev_29, stamped at 11-14) is minted and the floor override re-resolves on recompute. Alternatively, if the team intends to defer this (as with the documented Korea retro gap), add an explicit invariant/test asserting the Swiss floor activation date and a code comment in collect_schedule_boundaries noting that floor turn-ON effective dates are deliberately NOT discovered, so the omission is visible rather than silent. Verify the actual rate delta first (CH/LI rate_ieepa_recip in rev_29 vs rev_30) to confirm magnitude before prioritizing.

### [LOW] Largest §232 relocations (by_country/overrides/floors/deals + annex rate-mapping) have NO oracle test — only shape-checks + data-gated parity/invariant tests
_TEST QUALITY / PARITY-BLINDNESS / COVERAGE GAPS · refactor · verdict=uncertain · finder-confidence=high_
- **file:** `tests/test_authority_adapter.R` 141-194
- **problem:** Unlike the IEEPA reciprocal/fentanyl and exempt-set relocations — which have faithful oracle tests (test_ieepa_deblob.R, test_exempt_sets_in_spec.R copy master's exact code verbatim and assert adapter==oracle) — the §232 de-blobs do NOT. The steel/aluminum by_country overlay (.s232_blanket_by_country, authority_adapter.R:111-130, which merges steel_exempt + steel_country_overrides + pp$S232_COUNTRY_EXEMPTIONS with a date gate), the auto/wood deal layers (.s232_deal_layers, ISO/EU census expansion), and the entire Plank-4c §232 annex rate-mapping (tier->flat_rate, annex_3 floor_rate via pmax, country_overrides surcharge mode replace/max — authority_adapter.R:560-650, read at 06_calculate_rates.R:2137-2212) are tested ONLY by (a) shape assertions in test_authority_adapter.R (e.g. line 142 checks `by_country == {1220:0}` for a single steel_exempt=Canada case) and (b) the Russia/UK absolute tests in test_rate_calculation.R, which are skip_test-gated on snapshot_2026_rev_5.rds. There is no pure-logic test that runs master's old country_232 build / annex-rate application as an oracle and asserts the new adapter reproduces it. A relocation that silently moved a number (e.g. wrong merge precedence, a flipped date boundary, mode='max' vs 'replace' mis-scoping) and got baked into the re-frozen golden (70b6b97 -> 52dab78) would pass parity and, in a checkout lacking the gitignored snapshot data, would also pass test_rate_calculation (which skips). classify_s232_annex (the tier classifier) is well covered by test_classify_annex.R, but the rate-MAPPING that consumes the tier is not.
- **evidence:** grep shows §232 by_country/deals/annex referenced in tests only by test_authority_adapter.R (shape) and test_rate_calculation.R (data-gated Russia tests at lines 825-919). No file copies master's `country_232` build (git show 7779933:src/06_calculate_rates.R:1483-1535) or annex country-surcharge application as an oracle, in contrast to test_ieepa_deblob.R:47-100 which does exactly that for IEEPA. The annex de-blob is substantial (authority_adapter.R:560-650) and read at 06:2207-2212.
- **suggested fix (NOT applied):** Add a test_s232_deblob.R modeled on test_ieepa_deblob.R: copy master's country_232 exempt/override/config-exemption build and the annex country-override application as ORACLES, run them on a fixture covering each branch (exempt, override, config-exemption-active vs expired at the rev_date < expiry boundary, annex_1a/1b/2/3 tiers, UK replace deal, Russia max surcharge), and assert .s232_blanket_by_country / the annex rate_map reproduce them. Catches drift before it can be baked into the golden.
- **verifier:** PARTIALLY confirmed but materially overstated; verdict uncertain because the finding's central claim is half-right and half-wrong.

CONFIRMED facts (independently verified):
- No tests/test_s232_deblob.R exists. No test file copies master's country_232 build (git show 7779933:src/06_calculate_rates.R:1485-1535) or the annex application as a verbatim ORACLE and asserts adapter==oracle. This is in genuine contrast to tests/test_ieepa_deblob.R (oracle_country_ieepa, lines 47-100, verbatim copy of master's phase-collapse+floor override) and tests/test_exempt_sets_in_spec.R (oracle_ieepa_exempt/ora

### [LOW] CTY_CHINA=NULL would break the policy-from-specs/default identical() invariant (latent edge)
_Stacking Generalization · refactor · verdict=(pending) · finder-confidence=low_
- **file:** `src/authority_adapter.R, src/stacking.R` authority_adapter.R:636; stacking.R:184-226,141-157
- **problem:** The adapter bakes fentanyl exceptions with key `CTY_CHINA %||% 'china'` (authority_adapter.R:636). If CTY_CHINA were ever NULL, the spec would carry exceptions keyed 'china', and stacking_policy_from_specs(specs, NULL) would emit additive_countries='china', while default_stacking_policy(NULL) sets additive_countries = NULL which DROPS the list key entirely (assigning NULL to a list element removes it) — so the pinned identical() invariant would break and, worse, the China fentanyl full-rate passthrough would be keyed to a country code that never matches. Not reachable in practice (config sets CTY_CHINA='5700'), so latent.
- **evidence:** authority_adapter.R:636 `exceptions = setNames(list('additive'), CTY_CHINA %||% 'china')`. stacking.R:145-146 default sets `additive_countries = cty_china` (NULL -> key removed). stacking.R:216-222 from-specs sets entry$additive_countries when length(add_ctry)>0 ('china' is length 1). The two diverge under NULL.
- **suggested fix (NOT applied):** Drop the silent `%||% 'china'` fallback and fail loud if CTY_CHINA is unset, or thread the same cty_china constant into both the adapter exception key and the policy builders so they cannot disagree.

### [LOW] daily_part interval is computed with arrange(effective_date, revision) but validated with arrange(effective_date) — a latent tiebreak mismatch if two revisions ever share an effective_date
_PUBLISH / DAILY-AGGREGATION / PARITY-TOOLING INTEGRITY · refactor · verdict=(pending) · finder-confidence=medium_
- **file:** `scripts/build_revision.R` 93-101
- **problem:** The daily_part writer derives valid_until from `timeline_ordered <- timeline %>% arrange(effective_date, revision)` then `effective_date[idx+1]-1`. The part VALIDATOR (build_snapshot_intervals_for_daily, 09_daily_series.R:863-869) and the publish/assemble interval builders use `arrange(effective_date)` with NO revision tiebreak. If two revisions ever share an effective_date, the lead() neighbor (hence valid_until) can differ between writer and validator, causing the part to be rejected (interval mismatch) and the gather to stop. Today this is unreachable in baseline — config/revision_dates.csv has no duplicate dates, and bnd_/sched_ mints are strictly interior (owner_of returns NA for edge-coincident dates, timeline.R:184-190; Ch99 offset scan uses `> valid_from & <= valid_until`) — so no two revisions collide on a date. It is recorded as latent fragility, and it fails in the safe (loud) direction, not silently.
- **evidence:** scripts/build_revision.R:93 `timeline_ordered <- timeline %>% arrange(effective_date, revision)` vs src/09_daily_series.R:864-866 `rev_dates %>% filter(...) %>% arrange(effective_date) %>% mutate(valid_until = lead(effective_date)-1)` and src/rate_schema.R build_rev_intervals `arrange(effective_date)`. timeline.R:184-190 owner_of guards edge-coincidence.
- **suggested fix (NOT applied):** Make the part writer use the SAME ordering as the validator: drop the `revision` tiebreak (use `arrange(effective_date)`), or better, have build_revision.R compute its valid_from/valid_until by calling build_rev_intervals()/build_snapshot_intervals_for_daily() so writer and validator share one code path and can never disagree on the interval.

### [LOW] Streaming quality report stamps valid_from/valid_until but relies on the snapshot's own effective_date; an edge-coincident match() miss yields NA intervals only for unknown revisions (matches monolith, but undocumented)
_PUBLISH / DAILY-AGGREGATION / PARITY-TOOLING INTEGRITY · refactor · verdict=(pending) · finder-confidence=low_
- **file:** `src/quality_report.R` 408-413
- **problem:** build_quality_inputs_streaming stamps valid_from/valid_until from rev_intervals via `iv <- rev_intervals[match(ordered[i], rev_intervals$revision), ]`. If a snapshot on disk has a revision NOT present in rev_dates, match() returns NA, iv is an all-NA row, and that snapshot's valid_from/valid_until are stamped NA, inflating the schema NA% for those two columns. The monolith path (assemble_timeseries left_join by revision) would ALSO leave NA for an unknown revision, so the two paths agree — but the streaming code comments (lines 391-394) only justify stamping the intervals to avoid 100%-NA, and do not note the unknown-revision case. effective_date itself is taken straight from the raw snapshot (stamped at 06_calculate_rates.R:3060), so it matches the monolith. This is a minor robustness/clarity gap, not a baseline divergence.
- **evidence:** src/quality_report.R:411 `iv <- rev_intervals[match(ordered[i], rev_intervals$revision), ]`; rev_intervals is built from rev_dates filtered to `ordered`, and ordered = c(date-ordered revs, setdiff(rev_ids, date-ordered)) (line 387) can include rev_ids not in rev_dates. The monolith equivalent (00_build_timeseries.R:563-565 left_join by revision) yields the same NA, so parity holds.
- **suggested fix (NOT applied):** Either assert that every on-disk snapshot revision is present in rev_dates (fail loud, as the gather completeness gate already does upstream), or add a comment that NA intervals for an unknown revision are intentional and match the monolith left_join behavior.

## Refuted by verifier (recorded for completeness)

- ~~[NONE] Parallel-vs-serial output equivalence (a Phase-0 refactor) is explicitly NOT unit-tested; only the future.globals SHAPE is string-matched~~ (`tests/test_alt_runner_equivalence.R`) — REFUTED on two independent, load-bearing grounds.

(1) Wrong attribution — not introduced by the theseus refactor under review. The blob is byte-identical across master, the theseus branch point (7779933), and theseus HEAD: all ddaf4f3d04dd
- ~~[LOW] The §232 annex RATE-APPLICATION + country_overrides relocation (Plank 4c) is unit-tested only at the classifier level, not the rate-output level~~ (`tests/test_classify_annex.R`) — The finding's narrow premise is correct — /nfs/roberts/project/pi_nrs36/jar335/Repositories/tariff-rate-tracker/tests/test_classify_annex.R tests only classify_s232_annex (the tag), not rate output; its own header lines 9-10 say so. The cit
- ~~[NONE] Universal IEEPA Annex-II exempt list is now date-windowed (+ CSV fully rebuilt) — a real rate change vs master, hidden by the re-frozen golden~~ (`src/authority_adapter.R`) — The finding's mechanical claims are accurate but its central characterization (a "real rate change vs master, hidden by the re-frozen golden" with a false "bit-exact/VERBATIM" doc claim) is wrong because it compares against a STALE local ma
- ~~[LOW] Principal published mean_* daily rates are gated at the LOOSE etr tolerance (1e-7 relative), not the documented tight 1e-9 absolute 'rate' tolerance~~ (`src/parity.R`) — The finding's MECHANICAL claim is accurate but its FRAMING and SEVERITY are wrong, and its central attribution (introduced_by_refactor) is false.

CONFIRMED MECHANICS: src/parity.R:54-61 — classify_parity_column() routes columns matching `e
- ~~[NONE] extract_effective_date_offset regex only matches 'on or after Month DD, YYYY'; other future-effective phrasings silently pass the gate~~ (`src/rate_schema.R`) — The cited mechanism is accurately described and the code is real: src/rate_schema.R:214-241, extract_effective_date_offset keys solely on the regex 'on or after [A-Za-z]+ [0-9]{1,2}, [0-9]{4}' (lines 221-222), returns as.Date(NA) on no matc
- ~~[NONE] Doc mis-attributes the universal-exempt date filter to a 'fix-mapping correction'; master already had it (fix6)~~ (`docs/master_fix_integration.md`) — REFUTED. I independently traced the master commit chain and theseus port commits. The finding's central thesis — that docs/master_fix_integration.md mis-attributes the universal-list date filter and is "confused between fix5 and fix6" — doe
- ~~[NONE] Doc/comment 'VERBATIM from the old inline load' is inaccurate for the universal IEEPA exempt set; the date-gate is new vs the stated merge-base~~ (`src/authority_adapter.R`) — The finding misreads what the comment claims. The actual comment in /nfs/roberts/project/pi_nrs36/jar335/Repositories/tariff-rate-tracker/src/authority_adapter.R lines 311-313 reads: "Universal IEEPA Annex II exempt list (hts10 vector), dat
- ~~[NONE] Latent Date-vs-string inconsistency: universal gate drops empty-string ('') date cells that the country_eo gate would treat as always-active~~ (`src/authority_adapter.R`) — I verified the cited code in /nfs/roberts/project/pi_nrs36/jar335/Repositories/tariff-rate-tracker/src/authority_adapter.R. The two helpers exist exactly as described: .resolve_ieepa_exempt_products (lines 314-335) gates the universal list 
- ~~[NONE] Wood deal relocation reorders surcharge-before-floor; bit-exactness rests on an unverified per-country single-rate-type assumption~~ (`src/authority_adapter.R + src/06_calculate_rates.R`) — The finding's CODE DESCRIPTION is accurate, but its claimed PARITY DRIFT does not exist on any real input — it is an explicitly hypothetical, self-flagged-low-confidence latent assumption, not a bug.

Verified facts:
1. authority_adapter.R:
- ~~[NONE] Dead 'fall back to the autos §232 rate' arm in the new applicability_excluded block~~ (`src/06_calculate_rates.R`) — The literal facts check out but the finding's load-bearing characterization (a refactor-introduced "latent cross-authority foot-gun") is wrong. Verified directly:

CODE: src/06_calculate_rates.R:1574 does read `literal_rate = cfg$default_ra
- ~~[NONE] set_country_scope include={set} on ieepa_fentanyl does not restrict carveouts to the included set~~ (`src/scenario_ops.R`) — The cited code is quoted accurately. src/scenario_ops.R:248-257 (.ieepa_set_country_scope include branch) does compute `drop <- setdiff(names(bc), incl)` from by_country names only (L253) and then `drop_codes(pr, drop)` (L254); drop_codes (
- ~~[NONE] scrape_us_notes.R Nov-2025 'provided for in' regex hardening not ported (documented, accepted) — latent divergence risk if the scraper is re-run~~ (`src/scrape_us_notes.R`) — The finding's core technical claim is factually false. It asserts master hardened the floor-exemption regex with an optional (?:for\s+)? group that theseus lacks. I verified the actual code: master:src/scrape_us_notes.R line 521 and theseus
- ~~[NONE] test_policy_from_specs.R fixture omits section_301_forced_labor — the from-specs stacking read for that authority is never exercised~~ (`tests/test_policy_from_specs.R`) — The finding's load-bearing factual claims are wrong. (1) The adapter does NOT build section_301_forced_labor at baseline. src/authority_adapter.R:412-414 (.build_section_301_forced_labor) returns NULL when pp$section_301_forced_labor is abs